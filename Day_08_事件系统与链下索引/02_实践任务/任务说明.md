# Day 08 实践任务：事件系统与链下索引

## 📋 任务概览

本次实践包含 3 个任务，从简单到复杂，帮助你掌握事件系统的核心技能。

- **任务 1**：社交媒体平台（基础）⭐⭐☆☆☆
- **任务 2**：去中心化拍卖系统（中级）⭐⭐⭐☆☆
- **任务 3**：跨链桥事件追踪（高级）⭐⭐⭐⭐☆

**预计完成时间**：3-4 小时

---

## 任务 1：社交媒体平台 ⭐⭐☆☆☆

### 📝 任务描述

创建一个简单的社交媒体平台，用户可以发布帖子和点赞。使用事件系统记录所有社交活动。

### 🎯 功能要求

1. **用户注册**
   - 创建用户资料
   - 初始化事件句柄
   - 发射 `UserRegisteredEvent`

2. **发布帖子**
   - 创建新帖子
   - 自动递增帖子 ID
   - 发射 `PostCreatedEvent`

3. **点赞功能**
   - 用户可以点赞帖子
   - 发射 `PostLikedEvent`

4. **关注功能**
   - 用户可以关注其他用户
   - 发射 `UserFollowedEvent`

### 📊 事件结构

```move
/// 用户注册事件
struct UserRegisteredEvent has drop, store {
    user: address,
    username: vector<u8>,
    timestamp: u64
}

/// 帖子创建事件
struct PostCreatedEvent has drop, store {
    post_id: u64,
    author: address,
    content_hash: vector<u8>,  // IPFS 哈希
    timestamp: u64
}

/// 点赞事件
struct PostLikedEvent has drop, store {
    post_id: u64,
    liker: address,
    post_author: address,
    timestamp: u64
}

/// 关注事件
struct UserFollowedEvent has drop, store {
    follower: address,
    followee: address,
    timestamp: u64
}
```

### 💾 数据结构

```move
struct UserProfile has key {
    username: vector<u8>,
    followers_count: u64,
    following_count: u64,
    posts_count: u64,
    // 事件句柄
    register_events: event::EventHandle<UserRegisteredEvent>,
    post_events: event::EventHandle<PostCreatedEvent>,
    like_events: event::EventHandle<PostLikedEvent>,
    follow_events: event::EventHandle<UserFollowedEvent>
}

struct Post has store {
    id: u64,
    author: address,
    content_hash: vector<u8>,
    likes_count: u64,
    timestamp: u64
}

struct SocialPlatform has key {
    next_post_id: u64,
    total_posts: u64,
    total_users: u64
}
```

### ✅ 验收标准

- [ ] 用户可以注册并创建资料
- [ ] 用户可以发布帖子
- [ ] 用户可以点赞帖子
- [ ] 用户可以关注其他用户
- [ ] 所有操作都发射相应的事件
- [ ] 事件包含完整的元数据

### 💡 提示

1. 每个用户需要自己的 `UserProfile` 资源
2. 全局 `SocialPlatform` 用于管理平台级数据
3. 点赞时需要更新两个地方：帖子的点赞数和发射事件
4. 使用 `content_hash` 而不是完整内容，节省存储成本

---

## 任务 2：去中心化拍卖系统 ⭐⭐⭐☆☆

### 📝 任务描述

实现一个去中心化拍卖平台，支持创建拍卖、出价、结束拍卖等功能，并通过事件追踪所有拍卖活动。

### 🎯 功能要求

1. **创建拍卖**
   - 设置拍卖物品、起始价格、结束时间
   - 发射 `AuctionCreatedEvent`

2. **出价**
   - 用户提交出价
   - 验证出价高于当前最高价
   - 退还前一个最高出价
   - 发射 `BidPlacedEvent`

3. **结束拍卖**
   - 检查时间是否到期
   - 转移物品给最高出价者
   - 发射 `AuctionEndedEvent`

4. **取消拍卖**
   - 只有创建者可以取消（未有出价时）
   - 发射 `AuctionCancelledEvent`

### 📊 事件结构

```move
/// 拍卖创建事件
struct AuctionCreatedEvent has drop, store {
    auction_id: u64,
    seller: address,
    item_description: vector<u8>,
    starting_price: u64,
    end_time: u64,
    timestamp: u64
}

/// 出价事件
struct BidPlacedEvent has drop, store {
    auction_id: u64,
    bidder: address,
    bid_amount: u64,
    previous_highest_bid: u64,
    previous_highest_bidder: address,
    timestamp: u64
}

/// 拍卖结束事件
struct AuctionEndedEvent has drop, store {
    auction_id: u64,
    seller: address,
    winner: address,
    final_price: u64,
    timestamp: u64
}

/// 拍卖取消事件
struct AuctionCancelledEvent has drop, store {
    auction_id: u64,
    seller: address,
    reason: vector<u8>,
    timestamp: u64
}
```

### 💾 数据结构

```move
struct Auction has store {
    id: u64,
    seller: address,
    item_description: vector<u8>,
    starting_price: u64,
    current_highest_bid: u64,
    highest_bidder: address,
    end_time: u64,
    is_active: bool,
    created_at: u64
}

struct AuctionHouse has key {
    auctions: vector<Auction>,
    next_auction_id: u64,
    // 事件句柄
    creation_events: event::EventHandle<AuctionCreatedEvent>,
    bid_events: event::EventHandle<BidPlacedEvent>,
    end_events: event::EventHandle<AuctionEndedEvent>,
    cancel_events: event::EventHandle<AuctionCancelledEvent>
}

struct UserBids has key {
    active_bids: vector<u64>,  // auction_id 列表
    total_amount_locked: u64
}
```

### ✅ 验收标准

- [ ] 可以创建拍卖并设置参数
- [ ] 出价逻辑正确（价格验证、退款）
- [ ] 拍卖结束时正确转移资产
- [ ] 可以在无出价时取消拍卖
- [ ] 所有关键操作发射事件
- [ ] 事件包含前后状态变化

### 💡 提示

1. 使用 `timestamp::now_seconds()` 检查拍卖是否到期
2. 出价时需要锁定资金，可以简化为更新余额
3. 退还前一个出价者时要注意边界情况（第一次出价）
4. 考虑使用 `Option<address>` 处理可能不存在的最高出价者

### 🎓 进阶挑战

- 实现预留价格（reserve price）
- 支持多种代币出价
- 实现自动延长时间（如果最后时刻有出价）

---

## 任务 3：跨链桥事件追踪 ⭐⭐⭐⭐☆

### 📝 任务描述

实现一个跨链桥的事件系统，追踪资产跨链转移的完整生命周期，包括锁定、验证、释放等阶段。

### 🎯 功能要求

1. **锁定资产**
   - 用户在源链锁定资产
   - 生成唯一的转账 ID
   - 发射 `AssetsLockedEvent`

2. **提交证明**
   - 验证者提交跨链证明
   - 发射 `ProofSubmittedEvent`

3. **验证确认**
   - 多个验证者确认
   - 发射 `ValidationConfirmedEvent`

4. **释放资产**
   - 在目标链释放资产
   - 发射 `AssetsReleasedEvent`

5. **失败处理**
   - 验证失败时退还资产
   - 发射 `TransferFailedEvent`

### 📊 事件结构

```move
/// 资产锁定事件
struct AssetsLockedEvent has drop, store {
    transfer_id: u64,
    sender: address,
    target_chain: vector<u8>,
    target_address: vector<u8>,
    amount: u64,
    token_type: vector<u8>,
    timestamp: u64
}

/// 证明提交事件
struct ProofSubmittedEvent has drop, store {
    transfer_id: u64,
    validator: address,
    proof_hash: vector<u8>,
    timestamp: u64
}

/// 验证确认事件
struct ValidationConfirmedEvent has drop, store {
    transfer_id: u64,
    validator: address,
    confirmations_count: u64,
    required_confirmations: u64,
    timestamp: u64
}

/// 资产释放事件
struct AssetsReleasedEvent has drop, store {
    transfer_id: u64,
    recipient: address,
    amount: u64,
    total_confirmations: u64,
    timestamp: u64
}

/// 转账失败事件
struct TransferFailedEvent has drop, store {
    transfer_id: u64,
    sender: address,
    reason: vector<u8>,
    refunded: bool,
    timestamp: u64
}

/// 状态变更事件
struct StatusChangeEvent has drop, store {
    transfer_id: u64,
    old_status: u8,
    new_status: u8,
    changed_by: address,
    timestamp: u64
}
```

### 💾 数据结构

```move
/// 转账状态
const STATUS_PENDING: u8 = 0;
const STATUS_LOCKED: u8 = 1;
const STATUS_VALIDATING: u8 = 2;
const STATUS_CONFIRMED: u8 = 3;
const STATUS_RELEASED: u8 = 4;
const STATUS_FAILED: u8 = 5;

struct CrossChainTransfer has store {
    id: u64,
    sender: address,
    target_chain: vector<u8>,
    target_address: vector<u8>,
    amount: u64,
    token_type: vector<u8>,
    status: u8,
    confirmations: u64,
    required_confirmations: u64,
    validators: vector<address>,
    created_at: u64
}

struct Bridge has key {
    transfers: vector<CrossChainTransfer>,
    next_transfer_id: u64,
    total_locked: u64,
    total_released: u64,
    // 事件句柄
    lock_events: event::EventHandle<AssetsLockedEvent>,
    proof_events: event::EventHandle<ProofSubmittedEvent>,
    validation_events: event::EventHandle<ValidationConfirmedEvent>,
    release_events: event::EventHandle<AssetsReleasedEvent>,
    failure_events: event::EventHandle<TransferFailedEvent>,
    status_events: event::EventHandle<StatusChangeEvent>
}

struct ValidatorRegistry has key {
    validators: vector<address>,
    min_validators: u64
}
```

### ✅ 验收标准

- [ ] 可以锁定资产并发起跨链转账
- [ ] 验证者可以提交证明
- [ ] 多签验证机制正确
- [ ] 达到确认数后可以释放资产
- [ ] 失败场景正确处理和退款
- [ ] 完整的事件链追踪每个阶段
- [ ] 状态机转换逻辑正确

### 💡 提示

1. 使用状态机模式管理转账生命周期
2. 验证者列表可以预先设定
3. 每次状态变化都应发射 `StatusChangeEvent`
4. 考虑使用 `vector` 或 `simple_map` 存储验证者签名
5. 实现幂等性：相同验证者不能重复确认

### 🎓 进阶挑战

- 实现超时机制（自动退款）
- 支持部分释放（分批转账）
- 添加手续费机制
- 实现紧急暂停功能
- 添加黑名单检查

---

## 🔧 开发环境设置

### 1. 创建项目结构

```bash
cd Day_08_事件系统与链下索引/02_实践任务

# 文件结构
02_实践任务/
├── Move.toml
├── sources/
│   ├── task1_social_media.move
│   ├── task2_auction.move
│   └── task3_bridge.move
├── tests/
│   ├── task1_tests.move
│   ├── task2_tests.move
│   └── task3_tests.move
└── scripts/
    └── test_all.sh
```

### 2. Move.toml 配置

```toml
[package]
name = "Day08EventSystem"
version = "1.0.0"

[addresses]
day08 = "_"

[dependencies.AptosFramework]
git = "https://github.com/aptos-labs/aptos-core.git"
rev = "mainnet"
subdir = "aptos-move/framework/aptos-framework"

[dependencies.AptosStdlib]
git = "https://github.com/aptos-labs/aptos-core.git"
rev = "mainnet"
subdir = "aptos-move/framework/aptos-stdlib"
```

### 3. 测试命令

```bash
# 测试所有任务
aptos move test

# 测试单个任务
aptos move test --filter task1
aptos move test --filter task2
aptos move test --filter task3
```

---

## 📚 参考资料

### 官方文档
- [Aptos Events](https://aptos.dev/concepts/events/)
- [Event Module](https://aptos.dev/reference/move/?branch=mainnet&page=aptos-framework/doc/event.md)

### 示例代码
- 查看 `01_理论学习/代码示例.move`
- 参考 Day 07 的资源管理知识

---

## 💯 评分标准

### 任务 1（30 分）
- 功能完整性：15 分
- 事件设计合理性：10 分
- 代码质量：5 分

### 任务 2（35 分）
- 功能完整性：20 分
- 事件使用正确性：10 分
- 边界情况处理：5 分

### 任务 3（35 分）
- 功能完整性：20 分
- 状态机设计：8 分
- 事件追踪完整性：7 分

**总分：100 分**  
**及格分：70 分**

---

## 🎯 提交检查清单

完成任务后，请确认：

- [ ] 所有功能按要求实现
- [ ] 代码可以编译通过
- [ ] 编写了基本的测试用例
- [ ] 事件结构包含必要的字段
- [ ] 添加了适当的错误处理
- [ ] 代码有清晰的注释
- [ ] 遵循 Move 最佳实践

---

## 🆘 常见问题

### Q1: EventHandle 创建失败？
**A**: 确保在资源的构造函数中使用 `event::new_event_handle(account)` 创建。

### Q2: 如何测试事件是否发射？
**A**: 在测试中无法直接验证事件，但可以通过链下 API 查询，或检查相关的 counter 增加。

### Q3: 事件数据应该包含哪些信息？
**A**: 至少包含：操作者地址、时间戳、关键标识符（如 ID）、状态变化信息。

### Q4: 如何设计事件句柄的存储位置？
**A**: 通常放在全局资源中，如果是用户特定的事件，放在用户的资源中。

---

**开始你的实践之旅吧！记住：事件是连接链上和链下的桥梁！** 🚀
