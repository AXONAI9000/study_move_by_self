# 实践任务：完整 DEX 开发

## 🎯 项目目标

开发一个完整的去中心化交易所（DEX），包括后端合约、前端界面、部署和测试。这是你的第一个完整 DeFi 项目！

**预计时间**：5 小时  
**难度**：⭐⭐⭐⭐⭐

---

## 📋 项目需求

### 核心功能

1. **流动性管理**
   - 创建交易对
   - 添加流动性
   - 移除流动性
   - LP Token 管理

2. **代币交换**
   - Exact Input Swap
   - Exact Output Swap
   - 滑点保护
   - 手续费计算

3. **价格查询**
   - 预言机集成
   - TVL 计算
   - 价格展示

4. **用户界面**
   - 钱包连接
   - Swap 界面
   - 流动性管理界面
   - 池子列表

---

## 🔨 任务一：后端合约开发（3 小时）

### 1.1 实现核心模块（60 分钟）

**任务描述**：实现 DEX 核心模块和流动性池模块

**要实现的功能**：

```move
// sources/dex.move
module dex_addr::dex {
    /// 初始化 DEX
    public entry fun initialize(admin: &signer) {}
    
    /// 暂停/恢复
    public entry fun pause(admin: &signer) {}
    public entry fun unpause(admin: &signer) {}
    
    /// 配置管理
    public entry fun set_fee_rate(admin: &signer, new_rate: u64) {}
    
    /// 查询函数
    #[view]
    public fun get_fee_rate(): u64 {}
    #[view]
    public fun is_paused(): bool {}
}

// sources/liquidity_pool.move
module dex_addr::liquidity_pool {
    /// 创建池子
    public entry fun create_pool<X, Y>(creator: &signer) {}
    
    /// 添加流动性
    public entry fun add_liquidity<X, Y>(
        user: &signer,
        amount_x: u64,
        amount_y: u64,
        min_liquidity: u64
    ) {}
    
    /// 移除流动性
    public entry fun remove_liquidity<X, Y>(
        user: &signer,
        liquidity: u64,
        min_amount_x: u64,
        min_amount_y: u64
    ) {}
    
    /// 查询函数
    #[view]
    public fun get_reserves<X, Y>(): (u64, u64) {}
    #[view]
    public fun get_lp_token_supply<X, Y>(): u64 {}
}
```

**验收标准**：
- [ ] DEX 可以正确初始化
- [ ] 池子可以创建
- [ ] 添加流动性正确计算 LP Token
- [ ] 移除流动性正确返回代币
- [ ] 权限检查有效
- [ ] 暂停机制工作

### 1.2 实现 Swap 模块（60 分钟）

**任务描述**：实现完整的 Swap 功能

```move
// sources/swap_router.move
module dex_addr::swap_router {
    /// Exact Input Swap
    public entry fun swap_exact_input<X, Y>(
        user: &signer,
        amount_in: u64,
        min_amount_out: u64
    ) {}
    
    /// Exact Output Swap
    public entry fun swap_exact_output<X, Y>(
        user: &signer,
        amount_out: u64,
        max_amount_in: u64
    ) {}
    
    /// 计算输出
    #[view]
    public fun get_amount_out(
        amount_in: u64,
        reserve_in: u64,
        reserve_out: u64
    ): u64 {}
    
    /// 计算输入
    #[view]
    public fun get_amount_in(
        amount_out: u64,
        reserve_in: u64,
        reserve_out: u64
    ): u64 {}
}
```

**验收标准**：
- [ ] Swap 正确计算输出
- [ ] 手续费正确收取（0.3%）
- [ ] K 值不变性保持
- [ ] 滑点保护有效
- [ ] 事件正确发射

### 1.3 集成预言机（30 分钟）

**任务描述**：集成价格预言机

```move
// sources/oracle_integration.move
module dex_addr::oracle_integration {
    /// 获取代币美元价格
    #[view]
    public fun get_token_usd_price<T>(): u64 {}
    
    /// 计算池子 TVL
    #[view]
    public fun calculate_pool_tvl<X, Y>(): u64 {}
    
    /// 获取汇率
    #[view]
    public fun get_exchange_rate_usd<X, Y>(): u64 {}
}
```

**验收标准**：
- [ ] 可以查询代币价格
- [ ] TVL 计算正确
- [ ] 汇率计算准确

### 1.4 编写完整测试（30 分钟）

**任务描述**：编写全面的测试用例

```move
// tests/dex_integration_test.move
#[test_only]
module dex_addr::dex_integration_test {
    #[test]
    fun test_full_workflow() {
        // 1. 初始化
        // 2. 创建池子
        // 3. 添加流动性
        // 4. 执行 Swap
        // 5. 移除流动性
        // 6. 验证状态
    }
    
    #[test]
    fun test_multiple_swaps() {
        // 测试连续多次 swap
    }
    
    #[test]
    #[expected_failure]
    fun test_slippage_protection() {
        // 测试滑点保护
    }
}
```

**测试覆盖率要求**：
- [ ] 正常流程测试
- [ ] 边界情况测试
- [ ] 错误情况测试
- [ ] 集成测试

---

## 🎨 任务二：前端开发（1.5 小时）

### 2.1 设置项目（10 分钟）

```bash
cd frontend
npm install
# 或
yarn install
```

**需要的依赖**：
- React
- TypeScript
- Aptos SDK
- Wallet Adapter
- UI 库（Ant Design 或 Material-UI）

### 2.2 实现钱包连接（20 分钟）

**文件**：`frontend/src/components/WalletConnect.tsx`

**功能要求**：
- [ ] 连接 Petra 钱包
- [ ] 显示账户地址
- [ ] 显示代币余额
- [ ] 断开连接功能

### 2.3 实现 Swap 界面（40 分钟）

**文件**：`frontend/src/components/Swap.tsx`

**功能要求**：
- [ ] 代币选择器
- [ ] 数量输入框
- [ ] 实时价格预览
- [ ] 滑点设置
- [ ] 交换按钮
- [ ] 交易状态显示

**界面示例**：
```
┌─────────────────────────────────┐
│          Swap                   │
├─────────────────────────────────┤
│ From                            │
│ [1000        ] [USDC  ▼]        │
│ Balance: 5,000 USDC             │
├─────────────────────────────────┤
│            ⬇️                    │
├─────────────────────────────────┤
│ To (estimated)                  │
│ [49.5        ] [APT   ▼]        │
│ Balance: 100 APT                │
├─────────────────────────────────┤
│ 1 USDC = 0.0495 APT             │
│ Price Impact: 0.1%              │
│ Slippage: 0.5%                  │
│                                 │
│ [        Swap        ]          │
└─────────────────────────────────┘
```

### 2.4 实现流动性管理界面（30 分钟）

**文件**：`frontend/src/components/Liquidity.tsx`

**功能要求**：
- [ ] 选择交易对
- [ ] 输入两种代币数量
- [ ] 自动计算比例
- [ ] 显示 LP Token 数量
- [ ] 添加流动性按钮
- [ ] 移除流动性功能

---

## 🚀 任务三：部署和测试（30 分钟）

### 3.1 编译和测试（10 分钟）

```bash
# 编译
aptos move compile

# 运行测试
aptos move test

# 查看测试覆盖率
aptos move test --coverage
```

**验收标准**：
- [ ] 编译无错误
- [ ] 所有测试通过
- [ ] 测试覆盖率 > 80%

### 3.2 部署到测试网（10 分钟）

```bash
# 创建账户
aptos init --network testnet

# 获取测试币
aptos account fund-with-faucet --account default

# 部署合约
aptos move publish --named-addresses dex_addr=default
```

**验收标准**：
- [ ] 合约成功部署
- [ ] 记录合约地址
- [ ] 可以在浏览器查看

### 3.3 前端连接测试（10 分钟）

**步骤**：
1. 更新前端配置（合约地址）
2. 启动前端服务
3. 连接 Petra 钱包
4. 测试 Swap 功能
5. 测试添加流动性

**验收标准**：
- [ ] 钱包成功连接
- [ ] 可以查询池子信息
- [ ] 可以执行 Swap
- [ ] 可以添加/移除流动性
- [ ] 交易可以在浏览器查看

---

## 📦 交付物

### 1. 源代码

```
Day_19_Week3综合项目完整DEX/
├── sources/
│   ├── dex.move
│   ├── liquidity_pool.move
│   ├── swap_router.move
│   ├── oracle_integration.move
│   ├── math.move
│   ├── errors.move
│   └── events.move
├── tests/
│   ├── dex_test.move
│   ├── liquidity_test.move
│   ├── swap_test.move
│   └── integration_test.move
├── frontend/
│   └── src/
│       ├── components/
│       ├── hooks/
│       └── utils/
└── Move.toml
```

### 2. 文档

**README.md**：
- 项目介绍
- 功能列表
- 部署指南
- 使用说明

**ARCHITECTURE.md**：
- 系统架构
- 模块说明
- 数据流图

### 3. 测试报告

**TEST_REPORT.md**：
- 测试用例列表
- 测试结果
- 覆盖率报告

### 4. 部署信息

**DEPLOYMENT.md**：
- 合约地址
- 网络信息
- 前端 URL
- 演示视频/截图

---

## ✅ 检查清单

### 后端合约
- [ ] DEX 核心模块完成
- [ ] 流动性池模块完成
- [ ] Swap 路由模块完成
- [ ] 预言机集成完成
- [ ] 数学库完成
- [ ] 错误处理完善
- [ ] 事件系统完整
- [ ] 所有测试通过
- [ ] 代码有注释
- [ ] 编译无警告

### 前端界面
- [ ] 钱包连接功能
- [ ] Swap 界面完成
- [ ] 流动性管理完成
- [ ] 池子列表完成
- [ ] 价格展示正确
- [ ] 错误处理完善
- [ ] 加载状态显示
- [ ] 响应式设计
- [ ] 用户体验好

### 部署测试
- [ ] 合约成功部署
- [ ] 前端成功连接
- [ ] 端到端测试通过
- [ ] 文档完整
- [ ] 代码已提交 Git

---

## 💡 实现提示

### 后端技巧

1. **使用泛型简化代码**
```move
// 一个函数支持所有交易对
public fun add_liquidity<X, Y>(...) {}
// 而不是为每个交易对写一个函数
```

2. **事件要包含完整信息**
```move
struct SwapEvent has drop, store {
    user: address,
    token_in: String,
    token_out: String,
    amount_in: u64,
    amount_out: u64,
    timestamp: u64,
    // 包含所有必要信息
}
```

3. **使用 u128 避免溢出**
```move
let result = ((a as u128) * (b as u128) / (c as u128) as u64);
```

### 前端技巧

1. **使用 React Query 缓存**
```typescript
const { data } = useQuery(['pool', tokenX, tokenY], 
    () => getPoolReserves(tokenX, tokenY),
    { staleTime: 5000 }
);
```

2. **处理大数字**
```typescript
// 使用 BigNumber 或原生 BigInt
const amount = BigInt(amountStr) * BigInt(1e6);
```

3. **友好的错误提示**
```typescript
catch (error) {
    if (error.message.includes('slippage')) {
        toast.error('滑点过大，请增加滑点容忍度');
    } else {
        toast.error('交易失败：' + error.message);
    }
}
```

---

## 🆘 常见问题

### Q1: 如何测试没有真实代币的情况？

使用测试代币：
```move
#[test_only]
struct TestUSDC {}
#[test_only]
struct TestAPT {}
```

### Q2: 前端如何获取交易状态？

```typescript
const txHash = await client.submitTransaction(signedTx);
await client.waitForTransaction(txHash);
const txResult = await client.getTransactionByHash(txHash);
```

### Q3: 如何调试合约？

使用 `aptos move test -v` 查看详细日志

### Q4: 部署失败怎么办？

检查：
- 账户余额是否足够
- Move.toml 配置是否正确
- 代码是否编译通过

---

## 🎯 加分项（可选）

1. **多跳路由**
   - 实现 X -> Z -> Y 的路由
   - 自动寻找最优路径

2. **价格图表**
   - 使用 Chart.js 展示价格历史
   - K线图

3. **交易历史**
   - 显示用户交易记录
   - 支持导出

4. **移动端适配**
   - 响应式设计
   - 移动端钱包支持

5. **Gas 优化**
   - 批量操作
   - 减少存储

---

**预计完成时间**：5 小时  
**建议分配**：
- 后端：3 小时
- 前端：1.5 小时
- 部署：0.5 小时

**祝你成功完成你的第一个完整 DeFi 项目！🚀**
