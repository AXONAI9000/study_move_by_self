# 实践任务：Aptos 交易结构与解析

## 🎯 任务概述

通过四个递进式任务，你将构建一个完整的交易解析和分析工具。这些工具是MEV机器人、链上数据分析平台和交易监控系统的基础。

**总预计时间**：3小时

---

## 📋 任务清单

- [ ] **任务一**：交易解析器（60分钟）⭐⭐⭐
- [ ] **任务二**：事件过滤器（45分钟）⭐⭐
- [ ] **任务三**：交易分析工具（60分钟）⭐⭐⭐
- [ ] **任务四**：实时监控集成（15分钟）⭐

---

## 任务一：实现交易解析器（60分钟）⭐⭐⭐

### 📝 任务描述

实现一个完整的交易解析器，能够：
1. 解析 UserTransaction 的所有关键字段
2. 提取并验证交易基本信息
3. 处理 EntryFunction Payload
4. 支持批量解析

### 🎯 学习目标

- 理解 UserTransaction 结构
- 掌握 BCS 反序列化基础
- 学会提取 Payload 信息
- 实现交易验证逻辑

### 📌 任务要求

在 `sources/transaction_parser.move` 中实现以下功能：

#### 1.1 基础解析功能（20分钟）

```move
module day27::transaction_parser {
    use std::vector;
    use std::string::String;
    
    /// TODO: 定义 ParsedTransaction 结构
    /// 应包含：sender, sequence_number, max_gas, gas_price, 
    ///        expiration, chain_id
    struct ParsedTransaction has drop, copy {
        // 在这里添加字段
    }
    
    /// TODO: 解析交易基本信息
    /// 提示：提取所有关键字段并返回结构体
    public fun parse_transaction_basic(
        sender: address,
        sequence_number: u64,
        max_gas_amount: u64,
        gas_unit_price: u64,
        expiration_timestamp_secs: u64,
        chain_id: u8,
    ): ParsedTransaction {
        // 实现代码
    }
}
```

**测试用例**：
```move
#[test]
public fun test_parse_transaction_basic() {
    let parsed = parse_transaction_basic(
        @0x1,
        5,
        100_000,
        100,
        1700000000,
        1
    );
    // 验证字段正确性
}
```

#### 1.2 交易验证（20分钟）

```move
/// TODO: 验证交易有效性
/// 检查点：
/// - gas 参数合理性
/// - 过期时间未到
/// - chain_id 有效
/// - sequence_number > 0
public fun validate_transaction(txn: &ParsedTransaction): bool {
    // 实现验证逻辑
}

/// TODO: 计算交易费用
public fun calculate_transaction_fee(txn: &ParsedTransaction): u64 {
    // max_gas_amount * gas_unit_price
}
```

#### 1.3 EntryFunction 解析（20分钟）

```move
struct ParsedEntryFunction has drop {
    module_address: address,
    module_name: String,
    function_name: String,
    type_args_count: u64,
    args_count: u64,
}

/// TODO: 解析 EntryFunction Payload
/// 注意：这是简化版本，实际需要 BCS 反序列化
public fun parse_entry_function(
    module_address: address,
    module_name: vector<u8>,
    function_name: vector<u8>,
    type_args_count: u64,
    args_count: u64,
): ParsedEntryFunction {
    // 实现代码
}

/// TODO: 构造函数签名字符串
/// 格式: "0xADDRESS::module_name::function_name"
public fun build_function_signature(
    parsed: &ParsedEntryFunction
): String {
    // 实现代码
}
```

### ✅ 完成标准

- [ ] 所有结构体定义正确
- [ ] 基础解析功能实现
- [ ] 验证逻辑完整（至少4个检查点）
- [ ] EntryFunction 解析正确
- [ ] 函数签名构造准确
- [ ] 所有测试用例通过

### 💡 提示

1. **Gas 验证**：
   - max_gas_amount: 0 < value <= 2,000,000
   - gas_unit_price: value >= 100 Octas

2. **过期时间**：
   ```move
   use aptos_framework::timestamp;
   let current = timestamp::now_seconds();
   assert!(expiration > current, ERROR_EXPIRED);
   ```

3. **字符串拼接**：
   ```move
   use std::string::{Self, String};
   let sig = string::utf8(b"");
   string::append_utf8(&mut sig, b"0x");
   string::append(&mut sig, module_name);
   ```

---

## 任务二：实现事件过滤器（45分钟）⭐⭐

### 📝 任务描述

创建灵活的事件过滤系统，支持：
1. 按事件类型过滤
2. 按序列号范围过滤
3. 复合条件过滤
4. 统计分析

### 🎯 学习目标

- 理解事件结构
- 掌握过滤器模式
- 实现灵活查询
- 优化性能

### 📌 任务要求

在 `sources/event_filter.move` 中实现：

#### 2.1 事件数据结构（10分钟）

```move
module day27::event_filter {
    use std::vector;
    use std::string::{Self, String};
    use std::option::{Self, Option};
    
    /// TODO: 定义 EventData 结构
    struct EventData has drop, copy {
        sequence_number: u64,
        event_type: String,
        data_length: u64,
        // 可选：添加 timestamp, emitter 等字段
    }
    
    /// TODO: 创建事件数据
    public fun create_event(
        sequence_number: u64,
        event_type: vector<u8>,
        data_length: u64,
    ): EventData {
        // 实现代码
    }
}
```

#### 2.2 基础过滤（15分钟）

```move
/// TODO: 按类型过滤事件
public fun filter_by_type(
    events: &vector<EventData>,
    event_type: String,
): vector<EventData> {
    // 实现过滤逻辑
}

/// TODO: 按序列号范围过滤
public fun filter_by_sequence_range(
    events: &vector<EventData>,
    min_seq: u64,
    max_seq: u64,
): vector<EventData> {
    // 实现过滤逻辑
}

/// TODO: 统计特定类型事件数量
public fun count_by_type(
    events: &vector<EventData>,
    event_type: String,
): u64 {
    // 实现统计逻辑
}
```

#### 2.3 高级过滤器（20分钟）

```move
/// TODO: 定义过滤器结构
struct EventFilter has drop {
    type_filter: Option<String>,
    min_sequence: Option<u64>,
    max_sequence: Option<u64>,
    min_data_length: Option<u64>,
}

/// TODO: 创建过滤器
public fun create_filter(
    type_filter: Option<String>,
    min_sequence: Option<u64>,
    max_sequence: Option<u64>,
    min_data_length: Option<u64>,
): EventFilter {
    // 实现代码
}

/// TODO: 检查事件是否匹配过滤器
public fun matches(
    event: &EventData,
    filter: &EventFilter,
): bool {
    // 实现匹配逻辑
    // 检查每个过滤条件
}

/// TODO: 应用过滤器
public fun apply_filter(
    events: &vector<EventData>,
    filter: &EventFilter,
): vector<EventData> {
    // 遍历事件，应用过滤器
}
```

### ✅ 完成标准

- [ ] EventData 结构定义完整
- [ ] 基础过滤功能实现（3个函数）
- [ ] EventFilter 过滤器系统实现
- [ ] 支持多条件组合过滤
- [ ] 测试覆盖所有过滤场景
- [ ] 性能优化（避免不必要的复制）

### 💡 提示

1. **Option 使用**：
   ```move
   if (option::is_some(&filter.type_filter)) {
       let expected = option::borrow(&filter.type_filter);
       if (event.event_type != *expected) {
           return false
       };
   };
   ```

2. **避免不必要的复制**：
   ```move
   // 好：只在需要时复制
   vector::push_back(&mut result, *event);
   
   // 差：过早复制
   let copied = *event;
   if (matches) {
       vector::push_back(&mut result, copied);
   }
   ```

---

## 任务三：构建交易分析工具（60分钟）⭐⭐⭐

### 📝 任务描述

开发交易分析工具，提供：
1. 交易摘要生成
2. 批量分析
3. 统计指标计算
4. 异常检测

### 🎯 学习目标

- 数据聚合技术
- 统计分析实现
- 性能优化
- 实用工具开发

### 📌 任务要求

在 `sources/transaction_analyzer.move` 中实现：

#### 3.1 交易摘要（15分钟）

```move
module day27::transaction_analyzer {
    use std::vector;
    use std::string::String;
    use std::option::{Self, Option};
    use aptos_framework::timestamp;
    
    /// TODO: 定义交易摘要结构
    struct TransactionSummary has drop, copy {
        sender: address,
        sequence_number: u64,
        function_signature: String,
        gas_used: u64,
        success: bool,
        events_count: u64,
        timestamp: u64,
    }
    
    /// TODO: 创建交易摘要
    public fun create_summary(
        sender: address,
        sequence_number: u64,
        function_signature: String,
        gas_used: u64,
        success: bool,
        events_count: u64,
    ): TransactionSummary {
        // 实现代码
    }
}
```

#### 3.2 批量分析（20分钟）

```move
/// TODO: 批量分析交易
/// 输入：三个等长的向量
/// 输出：交易摘要向量
public fun analyze_batch(
    senders: vector<address>,
    sequences: vector<u64>,
    gas_amounts: vector<u64>,
): vector<TransactionSummary> {
    // 验证输入长度一致
    // 遍历并创建摘要
}
```

#### 3.3 统计分析（25分钟）

```move
/// TODO: 计算平均 gas 使用量
public fun calculate_average_gas(
    summaries: &vector<TransactionSummary>
): u64 {
    // 处理空向量
    // 累加 gas，计算平均值
}

/// TODO: 计算成功率（百分比）
public fun calculate_success_rate(
    summaries: &vector<TransactionSummary>
): u64 {
    // 统计成功交易数
    // 返回百分比 (0-100)
}

/// TODO: 查找最高 gas 交易
public fun find_highest_gas_transaction(
    summaries: &vector<TransactionSummary>
): Option<TransactionSummary> {
    // 遍历查找最大值
    // 返回 Option
}

/// TODO: 统计发送者分布
/// 返回每个发送者的交易数量
public fun count_transactions_per_sender(
    summaries: &vector<TransactionSummary>
): vector<address> {
    // 提取唯一发送者
    // 可以返回地址向量或创建更复杂的结构
}
```

### ✅ 完成标准

- [ ] TransactionSummary 结构完整
- [ ] 批量分析功能正确
- [ ] 4个统计函数实现
- [ ] 正确处理边界情况（空向量等）
- [ ] 使用 u128 避免溢出
- [ ] 所有测试通过

### 💡 提示

1. **避免溢出**：
   ```move
   // 使用 u128 进行累加
   let total = 0u128;
   total = total + (value as u128);
   // 最后转换回 u64
   ((total / count as u128) as u64)
   ```

2. **Option 使用**：
   ```move
   if (vector::is_empty(summaries)) {
       return option::none()
   };
   // ... 查找逻辑
   option::some(result)
   ```

---

## 任务四：实时监控集成（15分钟）⭐

### 📝 任务描述

将前面的功能整合，创建简单的实时监控接口。

### 📌 任务要求

在 `sources/monitor.move` 中实现：

```move
module day27::transaction_monitor {
    use day27::transaction_parser;
    use day27::event_filter;
    use day27::transaction_analyzer;
    
    /// TODO: 监控特定地址的交易
    public fun monitor_address(
        target_address: address,
        // ... 其他参数
    ) {
        // 使用解析器、过滤器和分析器
    }
    
    /// TODO: 检测高 gas 交易
    public fun detect_high_gas_transactions(
        threshold: u64,
        // ... 其他参数
    ) {
        // 过滤 gas > threshold 的交易
    }
}
```

### ✅ 完成标准

- [ ] 成功集成三个模块
- [ ] 提供至少2个监控功能
- [ ] 代码可编译运行

---

## 📊 测试要求

### 必须通过的测试

每个任务都应包含测试：

```move
#[test_only]
module day27::tests {
    use day27::transaction_parser;
    use day27::event_filter;
    use day27::transaction_analyzer;
    
    #[test]
    public fun test_complete_workflow() {
        // 1. 解析交易
        // 2. 过滤事件
        // 3. 分析结果
        // 4. 验证输出
    }
}
```

### 测试覆盖率

- [ ] 正常情况测试
- [ ] 边界情况（空向量、零值等）
- [ ] 错误情况（验证失败等）
- [ ] 集成测试

---

## 🎓 评分标准

### 任务一：交易解析器（30分）
- 结构定义（5分）
- 基础解析（10分）
- 验证逻辑（10分）
- EntryFunction 解析（5分）

### 任务二：事件过滤器（20分）
- 数据结构（5分）
- 基础过滤（8分）
- 高级过滤器（7分）

### 任务三：交易分析工具（30分）
- 摘要生成（5分）
- 批量分析（10分）
- 统计函数（15分）

### 任务四：监控集成（10分）
- 模块集成（5分）
- 监控功能（5分）

### 测试（10分）
- 测试覆盖率和质量

**总分：100分**
**及格线：70分**

---

## 📝 提交清单

完成后，确保以下文件存在且可运行：

```
sources/
├── transaction_parser.move     ✅
├── event_filter.move           ✅
├── transaction_analyzer.move   ✅
└── monitor.move                ✅

tests/
├── parser_test.move            ✅
├── filter_test.move            ✅
└── analyzer_test.move          ✅
```

---

## 💡 额外挑战（可选）

想要更多挑战？尝试：

1. **性能优化**
   - 实现事件索引
   - 使用二分查找
   - 批量处理优化

2. **功能扩展**
   - 支持 Multisig 解析
   - 实现 TWAP 计算
   - 添加缓存机制

3. **实用工具**
   - BCS 序列化/反序列化
   - 地址格式化
   - 交易哈希计算

---

## 🆘 需要帮助？

遇到问题时：

1. **查看示例代码**：`01_理论学习/代码示例.move`
2. **阅读文档**：`01_理论学习/核心概念.md`
3. **运行测试**：`aptos move test`
4. **检查错误信息**：仔细阅读编译器提示

---

**准备好了吗？开始构建你的交易解析工具！🚀**

**预计完成时间**：3小时
**难度**：⭐⭐⭐ 中等偏上
