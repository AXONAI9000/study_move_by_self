# Day 12 综合项目：链上投票系统

## 📋 项目概述

你将从零开始构建一个完整的链上投票系统，这是 Week 2 的综合项目。通过这个项目，你将整合本周学到的所有知识，包括泛型、资源管理、事件系统、错误处理和标准库。

**项目名称**：去中心化投票平台（Decentralized Voting Platform）

**核心功能**：
- 提案创建和管理
- 投票功能（赞成/反对/弃权）
- 投票结果统计
- 提案执行机制
- 事件记录和查询

**预计时间**：6-8 小时

---

## 🎯 项目需求

### 阶段 1：基础投票系统（必须完成）⭐⭐⭐⭐⭐

#### 1.1 系统初始化
```move
public entry fun initialize(admin: &signer)
```
- 创建治理配置
- 初始化投票系统
- 设置事件句柄
- 验证：只能初始化一次

#### 1.2 提案管理

**创建提案**
```move
public entry fun create_proposal(
    proposer: &signer,
    title: vector<u8>,
    description: vector<u8>,
    voting_period: u64,
)
```
- 提案包含标题、描述
- 设置投票开始和结束时间
- 分配唯一提案 ID
- 发射 `ProposalCreatedEvent` 事件
- 验证投票期限在合理范围内（1-7天）

**查询提案**
```move
#[view]
public fun get_proposal(proposal_id: u64): Proposal
```
- 返回提案的完整信息
- 包括当前投票统计

#### 1.3 投票功能

**投票**
```move
public entry fun vote(
    voter: &signer,
    proposal_id: u64,
    vote_type: u8,  // 0=反对, 1=赞成, 2=弃权
)
```
- 验证提案存在且处于投票期
- 检查用户未投过票
- 记录投票（一人一票）
- 更新提案统计
- 发射 `VoteCastEvent` 事件

**检查投票状态**
```move
#[view]
public fun has_voted(proposal_id: u64, voter: address): bool
```

#### 1.4 结果统计

**统计并更新提案状态**
```move
public entry fun finalize_proposal(proposal_id: u64)
```
- 检查投票期已结束
- 计算总票数
- 检查是否达到法定人数（quorum）
- 判断提案是否通过
- 更新提案状态（成功/失败）

---

### 阶段 2：进阶功能（推荐完成）⭐⭐⭐⭐☆

#### 2.1 基于代币的投票权

**数据结构**
```move
struct VoterInfo has key {
    token_balance: u64,
    delegated_to: Option<address>,
}
```

**加权投票**
```move
fun get_voting_power(voter: address): u64
```
- 根据持有代币数量计算投票权
- 支持委托投票

#### 2.2 提案执行

**可执行提案**
```move
struct ExecutableProposal has store {
    action_type: u8,
    target: address,
    parameters: vector<u8>,
}

public entry fun execute_proposal(
    executor: &signer,
    proposal_id: u64
)
```
- 验证提案已通过
- 添加时间锁（执行延迟）
- 执行链上操作
- 标记为已执行
- 发射 `ProposalExecutedEvent`

#### 2.3 委托投票

```move
public entry fun delegate_vote(
    delegator: &signer,
    delegate_to: address
)

public entry fun undelegate_vote(delegator: &signer)
```
- 允许用户委托投票权
- 记录委托关系
- 委托人投票时使用受托人的权重

#### 2.4 提案取消

```move
public entry fun cancel_proposal(
    proposer: &signer,
    proposal_id: u64
)
```
- 只有提案人可以取消
- 只能取消未开始投票的提案
- 退还创建费用（如果有）

---

### 阶段 3：高级功能（挑战）⭐⭐⭐⭐⭐

#### 3.1 投票快照

防止闪电贷攻击：
```move
struct VotingSnapshot has store {
    proposal_id: u64,
    snapshot_block: u64,
    voter_balances: Table<address, u64>,
}
```
- 在提案创建时记录代币余额快照
- 投票时使用快照余额而非实时余额

#### 3.2 多重签名提案

```move
struct MultisigProposal has store {
    required_signatures: u64,
    signers: vector<address>,
    signed: Table<address, bool>,
}
```
- 需要多个签名才能执行
- 适用于高风险操作

#### 3.3 提案类型系统

使用泛型支持不同类型的提案：
```move
struct TypedProposal<T: store> has store {
    id: u64,
    data: T,
    // ... 其他字段
}

// 不同类型的提案数据
struct ParameterChangeProposal has store {
    param_name: String,
    new_value: u64,
}

struct TreasuryProposal has store {
    recipient: address,
    amount: u64,
}
```

#### 3.4 投票历史查询

```move
struct VotingHistory has key {
    user_votes: Table<address, vector<Vote>>,
}

#[view]
public fun get_user_voting_history(user: address): vector<Vote>
```

---

## 📝 技术要求

### 必须实现
- ✅ 使用 `SmartTable` 存储提案（需要迭代）
- ✅ 使用 `Table` 存储投票记录（快速查询）
- ✅ 使用 `Option` 处理可选字段
- ✅ 使用 `String` 存储文本数据
- ✅ 完整的错误处理（至少 10 个错误码）
- ✅ 发射事件记录所有关键操作
- ✅ 使用 `timestamp` 处理时间
- ✅ 添加 `#[view]` 标记查询函数

### 推荐实现
- ✅ 使用能力系统保证资源安全
- ✅ 添加权限控制（admin 权限）
- ✅  参数验证（assert!）
- ✅ 代码注释完整

### 可选实现
- 使用 Move Prover 验证关键不变量
- 添加单元测试（至少覆盖主要功能）
- 实现批量操作

---

## 🏗️ 项目结构

```
Day_12_Week2综合项目投票系统/
├── Move.toml                    # 项目配置
├── sources/
│   ├── voting_system.move       # 主模块
│   ├── events.move              # 事件定义（可选，拆分模块）
│   └── governance.move          # 治理逻辑（可选）
├── tests/
│   └── voting_system_tests.move # 单元测试
└── scripts/
    ├── initialize.move          # 初始化脚本
    ├── create_proposal.move     # 创建提案脚本
    └── vote.move                # 投票脚本
```

---

## 💻 起始代码框架

在 `sources/voting_system.move` 中创建：

```move
module your_addr::voting_system {
    use std::signer;
    use std::string::{Self, String};
    use std::vector;
    use std::option::{Self, Option};
    use aptos_framework::timestamp;
    use aptos_framework::event::{Self, EventHandle};
    use aptos_framework::account;
    use aptos_std::table::{Self, Table};
    use aptos_std::smart_table::{Self, SmartTable};

    // ==================== 错误码 ====================
    const ERROR_NOT_INITIALIZED: u64 = 1;
    const ERROR_ALREADY_INITIALIZED: u64 = 2;
    const ERROR_PROPOSAL_NOT_FOUND: u64 = 3;
    const ERROR_INVALID_VOTING_PERIOD: u64 = 4;
    const ERROR_VOTING_NOT_STARTED: u64 = 5;
    const ERROR_VOTING_ENDED: u64 = 6;
    const ERROR_ALREADY_VOTED: u64 = 7;
    const ERROR_INVALID_VOTE_TYPE: u64 = 8;
    const ERROR_PROPOSAL_NOT_PASSED: u64 = 9;
    const ERROR_ALREADY_EXECUTED: u64 = 10;
    // TODO: 添加更多错误码

    // ==================== 常量 ====================
    const STATUS_PENDING: u8 = 0;
    const STATUS_ACTIVE: u8 = 1;
    const STATUS_SUCCEEDED: u8 = 2;
    const STATUS_DEFEATED: u8 = 3;
    const STATUS_EXECUTED: u8 = 4;

    const VOTE_NO: u8 = 0;
    const VOTE_YES: u8 = 1;
    const VOTE_ABSTAIN: u8 = 2;

    // ==================== 数据结构 ====================
    
    struct Proposal has store, drop, copy {
        id: u64,
        title: String,
        description: String,
        proposer: address,
        created_at: u64,
        voting_start: u64,
        voting_end: u64,
        yes_votes: u64,
        no_votes: u64,
        abstain_votes: u64,
        status: u8,
        executable: bool,
        executed: bool,
    }

    struct Vote has store, drop, copy {
        proposal_id: u64,
        voter: address,
        vote_type: u8,
        voting_power: u64,
        timestamp: u64,
    }

    struct GovernanceConfig has key {
        admin: address,
        min_voting_period: u64,
        max_voting_period: u64,
        quorum_percentage: u64,
        approval_threshold: u64,
        total_voting_power: u64,
    }

    struct VotingSystem has key {
        proposals: SmartTable<u64, Proposal>,
        votes: Table<u64, Table<address, Vote>>,
        next_proposal_id: u64,
        total_proposals: u64,
        active_proposals: u64,
    }

    // TODO: 添加事件结构

    struct GovernanceEvents has key {
        // TODO: 添加事件句柄
    }

    // ==================== 公共函数 ====================
    
    /// TODO: 实现初始化函数
    public entry fun initialize(admin: &signer) {
        // 你的代码
    }

    /// TODO: 实现创建提案
    public entry fun create_proposal(
        proposer: &signer,
        title: vector<u8>,
        description: vector<u8>,
        voting_period: u64,
    ) {
        // 你的代码
    }

    /// TODO: 实现投票功能
    public entry fun vote(
        voter: &signer,
        proposal_id: u64,
        vote_type: u8,
    ) {
        // 你的代码
    }

    /// TODO: 实现结果统计
    public entry fun finalize_proposal(proposal_id: u64) {
        // 你的代码
    }

    // ==================== 查询函数 ====================
    
    /// TODO: 实现查询函数
    #[view]
    public fun get_proposal(proposal_id: u64): Proposal {
        // 你的代码
    }

    #[view]
    public fun has_voted(proposal_id: u64, voter: address): bool {
        // 你的代码
    }

    // ==================== 辅助函数 ====================
    
    fun assert_initialized() {
        // 你的代码
    }

    fun get_voting_power(_voter: address): u64 {
        1  // 简化版：每人 1 票
    }
}
```

---

## ✅ 验证标准

### 功能完整性（50分）
- [ ] 系统初始化（5分）
- [ ] 创建提案（10分）
- [ ] 投票功能（15分）
- [ ] 结果统计（10分）
- [ ] 查询功能（10分）

### 代码质量（30分）
- [ ] 错误处理完整（10分）
- [ ] 代码注释清晰（5分）
- [ ] 命名规范（5分）
- [ ] 结构清晰（10分）

### 事件系统（10分）
- [ ] 定义完整的事件（5分）
- [ ] 正确发射事件（5分）

### 测试（10分）
- [ ] 通过编译（5分）
- [ ] 有测试用例（5分）

**总分 70 分以上通过**

---

## 🧪 测试指南

### 编译测试
```bash
cd Day_12_Week2综合项目投票系统
aptos move compile
```

### 单元测试示例

在 `tests/voting_system_tests.move` 中：

```move
#[test_only]
module your_addr::voting_system_tests {
    use std::signer;
    use aptos_framework::timestamp;
    use your_addr::voting_system;

    #[test(admin = @your_addr)]
    fun test_initialize(admin: &signer) {
        // 设置时间戳
        timestamp::set_time_has_started_for_testing(&account::create_account_for_test(@0x1));
        
        // 初始化
        voting_system::initialize(admin);
        
        // 验证
        let (admin_addr, quorum, threshold, _, _) = voting_system::get_config();
        assert!(admin_addr == signer::address_of(admin), 0);
        assert!(quorum == 30, 1);
        assert!(threshold == 50, 2);
    }

    #[test(admin = @your_addr, proposer = @0x123)]
    fun test_create_and_vote(admin: &signer, proposer: &signer) {
        // 初始化
        timestamp::set_time_has_started_for_testing(&account::create_account_for_test(@0x1));
        voting_system::initialize(admin);
        
        // 创建提案
        voting_system::create_proposal(
            proposer,
            b"Test Proposal",
            b"This is a test",
            86400,  // 1 day
        );
        
        // 投票
        voting_system::vote(proposer, 1, 1);  // 赞成
        
        // 验证
        assert!(voting_system::has_voted(1, signer::address_of(proposer)), 0);
        let proposal = voting_system::get_proposal(1);
        assert!(proposal.yes_votes == 1, 1);
    }

    // TODO: 添加更多测试
}
```

### 运行测试
```bash
aptos move test
```

---

## 🚀 部署指南

### 1. 配置账户
```bash
aptos init
```

### 2. 编译
```bash
aptos move compile
```

### 3. 发布到测试网
```bash
aptos move publish
```

### 4. 初始化系统
```bash
aptos move run --function-id 'your_addr::voting_system::initialize'
```

### 5. 创建测试提案
```bash
aptos move run \
  --function-id 'your_addr::voting_system::create_proposal' \
  --args string:"My First Proposal" \
       string:"Let's vote on this!" \
       u64:86400
```

---

## 💡 开发提示

### 1. 分步实现
不要一次性实现所有功能，建议顺序：
1. 先实现数据结构定义
2. 再实现初始化和创建提案
3. 然后实现投票功能
4. 最后添加统计和执行

### 2. 频繁测试
每实现一个功能就测试：
```bash
aptos move compile && aptos move test
```

### 3. 参考示例代码
查看 `01_理论学习/代码示例.move`

### 4. 调试技巧
- 使用 `assert!` 验证假设
- 添加详细的错误信息
- 使用 `#[test]` 隔离测试每个功能

### 5. Gas 优化
- 使用 SmartTable 而不是 vector 存储大量数据
- 批量操作减少状态访问
- 避免不必要的重复计算

---

## 📚 参考资源

### 示例代码
- `01_理论学习/代码示例.move` - 完整实现参考

### 官方文档
- [Aptos Move Book](https://aptos.dev/move/book/)
- [Timestamp Module](https://aptos.dev/reference/move?branch=mainnet&page=aptos-framework/doc/timestamp.md)
- [Event Module](https://aptos.dev/reference/move?branch=mainnet&page=aptos-framework/doc/event.md)

### 开源项目
- [Aptos DAO](https://github.com/aptos-labs/aptos-core/tree/main/aptos-move/move-examples/dao)

---

## 🎯 提交检查清单

提交前确认：
- [ ] 代码能够编译通过
- [ ] 实现了所有基础功能
- [ ] 添加了完整的注释
- [ ] 错误处理完善
- [ ] 有事件系统
- [ ] 通过了测试
- [ ] （可选）部署到测试网

---

**预计完成时间**：6-8 小时  
**难度评级**：⭐⭐⭐⭐☆

完成这个项目后，你就正式成为 Move 中级开发者了！加油！🚀💪
