# Day 06 每日考试 - 选择题

**总分：50 分（每题 5 分）**  
**时间：30 分钟**

---

## 第 1 题

以下哪个结构体的能力声明是**错误**的？

```move
A. struct Config has copy, drop, store { value: u64 }
B. struct Coin has store { value: u64 }
C. struct Account has key { balance: u64 }
D. struct Token has copy, store { amount: u64 }
```

**答案：** 见答案解析

---

## 第 2 题

下列代码会产生什么错误？

```move
struct Resource {
    value: u64
}

fun test() {
    let r = Resource { value: 100 };
    let r2 = r;
    let r3 = r;  // 这一行
}
```

A. 编译通过，没有错误  
B. `Resource` 缺少 `copy` 能力，`r` 已被移动  
C. `Resource` 缺少 `drop` 能力  
D. 类型不匹配

**答案：** 见答案解析

---

## 第 3 题

为什么代币类型（Coin）不应该有 `copy` 能力？

A. 为了节省内存空间  
B. 为了防止双花攻击  
C. 为了提高执行速度  
D. Move 语言的强制要求

**答案：** 见答案解析

---

## 第 4 题

Hot Potato 模式的核心特点是什么？

A. 结构体有所有能力  
B. 结构体没有任何能力  
C. 结构体只有 `drop` 能力  
D. 结构体只有 `copy` 能力

**答案：** 见答案解析

---

## 第 5 题

以下泛型函数声明中，哪个是**正确**的？

```move
A. public fun process<T>(value: T): T { value }
B. public fun duplicate<T: copy>(value: T): (T, T) { (value, value) }
C. public fun store<T: drop>(value: T) { /* ... */ }
D. 以上都正确
```

**答案：** 见答案解析

---

## 第 6 题

如果一个结构体的某个字段没有 `drop` 能力，那么这个结构体：

A. 必须也没有 `drop` 能力  
B. 可以有 `drop` 能力  
C. 必须有 `key` 能力  
D. 必须有 `store` 能力

**答案：** 见答案解析

---

## 第 7 题

以下代码的作用是什么？

```move
struct Witness has drop {}

public fun create_witness(): Witness {
    Witness {}
}
```

A. 创建一个可存储的凭证  
B. 创建一个一次性使用的授权凭证  
C. 创建一个永久有效的权限  
D. 代码有错误，无法编译

**答案：** 见答案解析

---

## 第 8 题

在 Move 中，哪种能力允许类型作为全局存储的顶层对象？

A. `copy`  
B. `drop`  
C. `store`  
D. `key`

**答案：** 见答案解析

---

## 第 9 题

下列关于泛型约束的说法，哪个是**错误**的？

A. `<T: copy>` 表示 T 必须具有 copy 能力  
B. `<T: copy + drop>` 表示 T 必须同时具有 copy 和 drop 能力  
C. `<T: store>` 表示 T 可以存储在全局存储中  
D. 泛型约束是可选的，所有类型都可以不加约束

**答案：** 见答案解析

---

## 第 10 题

以下哪个操作**不需要** T 具有 `copy` 能力？

```move
A. let x = value; let y = value;
B. vector::push_back(&mut vec, value);
C. return value;
D. *&value
```

**答案：** 见答案解析

---

## 第 11 题（附加题，10 分）

阅读以下代码，判断哪个说法是**正确**的：

```move
struct Container<T: store> has key {
    items: vector<T>
}

struct Item has store {
    value: u64
}

struct NonStorable {
    value: u64
}
```

A. 可以创建 `Container<Item>`  
B. 可以创建 `Container<NonStorable>`  
C. `Container` 可以存储任何类型  
D. `Container` 的泛型约束是多余的

**答案：** 见答案解析

---

## 计分说明

- 前 10 题每题 5 分，共 50 分
- 第 11 题附加题 10 分
- **及格分数**：35 分（70%）
- **优秀分数**：45 分（90%）

---

## 答题说明

1. 请在独立的纸上或文档中记录你的答案
2. 完成后对照 `答案解析.md` 检查
3. 仔细阅读解析，理解每道题的考点
4. 错题要重点复习相关知识点

**建议完成时间**：30 分钟  
**实际用时**：________

祝你考试顺利！🎓
