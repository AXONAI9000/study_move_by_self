# Day 33 实践任务：构建套利机器人 v1

## 📋 任务概述

今天你将构建一个完整的套利机器人系统，包括链上合约和链下监控程序。这是一个综合性的大项目，需要整合前几天学到的所有知识。

**预计完成时间**: 5小时  
**难度等级**: ⭐⭐⭐⭐⭐

---

## 🎯 任务目标

完成以下三个主要任务：

1. **任务一**: 完善链上合约（1.5小时）
2. **任务二**: 开发监控系统（2小时）
3. **任务三**: 集成测试和部署（1.5小时）

---

## 📝 任务一：完善链上合约（1.5小时）

### 1.1 实现完整的套利执行器

在 `sources/arbitrage_executor.move` 中实现：

```move
module arbitrage_bot_addr::arbitrage_executor {
    use std::signer;
    use std::string::String;
    use aptos_framework::coin::{Self, Coin};
    use arbitrage_bot_addr::dex_adapter;
    use arbitrage_bot_addr::flashloan;
    
    /// TODO 1: 实现简单套利执行
    /// 要求:
    /// - 在 dex_a 买入代币
    /// - 在 dex_b 卖出代币
    /// - 验证利润 >= min_profit
    /// - 处理滑点保护
    public entry fun execute_simple<TokenIn, TokenOut>(
        executor: &signer,
        dex_a: String,
        dex_b: String,
        amount: u64,
        min_profit: u64
    ) {
        // 你的代码...
    }
    
    /// TODO 2: 实现三角套利
    /// 要求:
    /// - 执行三次交换: A -> B -> C -> A
    /// - 确保最终金额 > 初始金额
    /// - 记录每步的金额变化
    public entry fun execute_triangular<TokenA, TokenB, TokenC>(
        executor: &signer,
        dex: String,
        amount: u64,
        min_profit: u64
    ) {
        // 你的代码...
    }
    
    /// TODO 3: 实现闪电贷套利
    /// 要求:
    /// - 借出闪电贷
    /// - 执行套利
    /// - 归还本金 + 费用
    /// - 提取利润
    public entry fun execute_with_flashloan<TokenIn, TokenOut>(
        executor: &signer,
        borrow_amount: u64,
        dex_a: String,
        dex_b: String,
        min_profit: u64
    ) {
        // 你的代码...
    }
}
```

**提示**:
- 使用 `dex_adapter::swap_exact_input` 进行交换
- 使用 `coin::extract` 和 `coin::merge` 管理代币
- 使用 `assert!` 进行错误检查
- 发出事件记录执行结果

### 1.2 添加安全检查模块

创建 `sources/safety_checks.move`:

```move
module arbitrage_bot_addr::safety_checks {
    /// TODO 4: 实现滑点检查
    /// 要求:
    /// - 计算实际输出与预期输出的差异
    /// - 如果滑点 > max_slippage_bps，则 abort
    public fun check_slippage(
        expected_output: u64,
        actual_output: u64,
        max_slippage_bps: u64
    ) {
        // 你的代码...
    }
    
    /// TODO 5: 实现利润验证
    /// 要求:
    /// - 计算净利润（扣除 Gas）
    /// - 验证净利润 >= 最小要求
    public fun verify_profit(
        amount_in: u64,
        amount_out: u64,
        gas_cost: u64,
        min_profit: u64
    ): u64 {
        // 你的代码...
    }
    
    /// TODO 6: 实现超时检查
    /// 要求:
    /// - 检查当前时间 <= 截止时间
    /// - 如果超时，则 abort
    public fun check_deadline(deadline: u64) {
        // 你的代码...
    }
}
```

### 1.3 编写测试

在 `tests/arbitrage_tests.move` 中：

```move
#[test_only]
module arbitrage_bot_addr::arbitrage_tests {
    use arbitrage_bot_addr::arbitrage_executor;
    use arbitrage_bot_addr::flashloan;
    
    #[test(executor = @0x123)]
    fun test_simple_arbitrage(executor: &signer) {
        // TODO 7: 测试简单套利
        // 设置测试环境
        // 执行套利
        // 验证结果
    }
    
    #[test(executor = @0x123)]
    fun test_triangular_arbitrage(executor: &signer) {
        // TODO 8: 测试三角套利
    }
    
    #[test(executor = @0x123)]
    fun test_flashloan_arbitrage(executor: &signer) {
        // TODO 9: 测试闪电贷套利
    }
}
```

**验收标准**:
- [ ] 所有合约编译通过 (`aptos move compile`)
- [ ] 所有测试通过 (`aptos move test`)
- [ ] 代码有完整的注释和错误处理

---

## 💻 任务二：开发监控系统（2小时）

### 2.1 实现价格监控器

创建 `scripts/price_monitor.ts`:

```typescript
import { AptosClient } from 'aptos';
import WebSocket from 'ws';

interface PriceUpdate {
  dex: string;
  pair: string;
  price: number;
  liquidity: number;
  timestamp: number;
}

class PriceMonitor {
  // TODO 10: 实现价格监控
  // 要求:
  // - 连接到多个 DEX 的 WebSocket
  // - 监听价格更新事件
  // - 缓存最新价格
  // - 通知订阅者

  constructor(dexConfigs: DexConfig[]) {
    // 你的代码...
  }

  async start() {
    // 你的代码...
  }

  onPriceUpdate(callback: (update: PriceUpdate) => void) {
    // 你的代码...
  }
}

// TODO: 实现并测试
```

### 2.2 实现机会发现器

创建 `scripts/opportunity_finder.ts`:

```typescript
interface ArbitrageOpportunity {
  type: 'simple' | 'triangular';
  dexes: string[];
  tokens: string[];
  amounts: number[];
  estimated_profit: number;
  confidence: number;
}

class OpportunityFinder {
  // TODO 11: 实现机会发现
  // 要求:
  // - 分析价格数据，寻找套利机会
  // - 计算最优交易量
  // - 估算利润和 Gas 成本
  // - 评估置信度

  findSimpleArbitrage(
    pair: string,
    prices: Map<string, PriceUpdate>
  ): ArbitrageOpportunity[] {
    // 你的代码...
  }

  findTriangularArbitrage(
    dex: string,
    pairs: Map<string, PriceUpdate>
  ): ArbitrageOpportunity[] {
    // 你的代码...
  }
}

// TODO: 实现并测试
```

### 2.3 实现执行引擎

创建 `scripts/execution_engine.ts`:

```typescript
import { AptosClient, AptosAccount } from 'aptos';

interface ExecutionResult {
  success: boolean;
  txn_hash?: string;
  profit?: number;
  error?: string;
}

class ExecutionEngine {
  // TODO 12: 实现执行引擎
  // 要求:
  // - 构建交易 payload
  // - 签名并提交交易
  // - 等待确认
  // - 解析结果
  // - 处理错误和重试

  constructor(
    private client: AptosClient,
    private account: AptosAccount
  ) {}

  async execute(
    opportunity: ArbitrageOpportunity
  ): Promise<ExecutionResult> {
    // 你的代码...
  }

  private async buildTransaction(opp: ArbitrageOpportunity) {
    // 你的代码...
  }

  private async submitWithRetry(signedTxn: any) {
    // 你的代码...
  }
}

// TODO: 实现并测试
```

### 2.4 集成主程序

创建 `scripts/main.ts`:

```typescript
// TODO 13: 实现主程序
// 要求:
// - 初始化所有模块
// - 启动价格监控
// - 持续寻找和执行套利
// - 记录统计信息
// - 优雅退出

async function main() {
  console.log('🤖 套利机器人启动...');
  
  // 1. 初始化
  const config = loadConfig();
  const monitor = new PriceMonitor(config.dexes);
  const finder = new OpportunityFinder();
  const executor = new ExecutionEngine(/* ... */);
  
  // 2. 启动监控
  monitor.onPriceUpdate(async (update) => {
    // 寻找机会
    // 评估机会
    // 执行套利
  });
  
  await monitor.start();
  
  // 3. 保持运行
  process.on('SIGINT', () => {
    console.log('👋 机器人停止');
    process.exit(0);
  });
}

main().catch(console.error);
```

**验收标准**:
- [ ] 价格监控正常工作，能实时获取价格
- [ ] 能够发现简单套利和三角套利机会
- [ ] 能够成功执行套利交易
- [ ] 有完整的日志和错误处理

---

## 🚀 任务三：集成测试和部署（1.5小时）

### 3.1 编写集成测试

创建 `tests/integration_test.ts`:

```typescript
import { AptosClient, AptosAccount, FaucetClient } from 'aptos';

describe('套利机器人集成测试', () => {
  // TODO 14: 设置测试环境
  
  test('应该能发现简单套利机会', async () => {
    // 1. 在两个 DEX 创建价格差异
    // 2. 运行机会发现器
    // 3. 验证发现了机会
  });

  test('应该能成功执行简单套利', async () => {
    // 1. 准备测试账户和代币
    // 2. 执行套利
    // 3. 验证利润
  });

  test('应该能处理执行失败', async () => {
    // 1. 模拟失败场景（滑点过大等）
    // 2. 验证错误处理
    // 3. 验证状态回滚
  });
});

// TODO: 实现所有测试
```

### 3.2 部署到测试网

创建 `scripts/deploy.ts`:

```typescript
// TODO 15: 实现部署脚本
// 要求:
// - 编译合约
// - 部署到 Aptos 测试网
// - 初始化模块
// - 添加初始流动性
// - 验证部署

async function deploy() {
  console.log('📦 开始部署...');
  
  // 1. 编译
  execSync('aptos move compile');
  
  // 2. 部署
  const result = execSync(
    'aptos move publish --profile testnet'
  ).toString();
  
  console.log('✅ 部署成功');
  console.log(result);
  
  // 3. 初始化
  // ...
}

deploy().catch(console.error);
```

### 3.3 压力测试

创建 `scripts/stress_test.ts`:

```typescript
// TODO 16: 实现压力测试
// 要求:
// - 模拟高频价格更新
// - 测试并发执行
// - 监控性能指标
// - 记录结果

async function stressTest() {
  console.log('🔥 开始压力测试...');
  
  const DURATION_MINUTES = 10;
  const UPDATES_PER_SECOND = 100;
  
  // 你的代码...
}
```

### 3.4 创建控制面板（可选）

如果时间充裕，创建一个简单的 Web 界面：

```typescript
// frontend/src/App.tsx
import React, { useState, useEffect } from 'react';

function Dashboard() {
  const [stats, setStats] = useState({
    total_profit: 0,
    executions: 0,
    success_rate: 0,
  });

  const [opportunities, setOpportunities] = useState([]);

  // TODO 17: 实现控制面板
  // - 显示实时统计
  // - 显示发现的机会
  // - 显示执行历史
  // - 参数配置界面

  return (
    <div className="dashboard">
      <h1>🤖 套利机器人控制面板</h1>
      {/* 你的 UI 代码 */}
    </div>
  );
}
```

**验收标准**:
- [ ] 所有集成测试通过
- [ ] 成功部署到测试网
- [ ] 压力测试结果良好（无崩溃，性能稳定）
- [ ] （可选）控制面板正常工作

---

## 📊 提交要求

完成后，你的项目应包含：

### 文件清单
```
Day_33_Week5综合项目套利机器人v1/
├─ sources/
│  ├─ arbitrage_bot.move         ✅ 主合约
│  ├─ arbitrage_executor.move    ✅ 执行器
│  ├─ flashloan.move             ✅ 闪电贷
│  ├─ dex_adapter.move           ✅ DEX 适配器
│  └─ safety_checks.move         ✅ 安全检查
├─ tests/
│  ├─ arbitrage_tests.move       ✅ Move 测试
│  └─ integration_test.ts        ✅ 集成测试
├─ scripts/
│  ├─ price_monitor.ts           ✅ 价格监控
│  ├─ opportunity_finder.ts      ✅ 机会发现
│  ├─ execution_engine.ts        ✅ 执行引擎
│  ├─ main.ts                    ✅ 主程序
│  ├─ deploy.ts                  ✅ 部署脚本
│  └─ stress_test.ts             ✅ 压力测试
├─ Move.toml                     ✅ 配置
├─ package.json                  ✅ 依赖
└─ README.md                     ✅ 文档
```

### 功能清单
- [ ] 简单套利执行
- [ ] 三角套利执行
- [ ] 闪电贷套利执行
- [ ] 实时价格监控
- [ ] 套利机会发现
- [ ] 自动执行引擎
- [ ] 滑点保护
- [ ] 错误处理和重试
- [ ] 统计和日志
- [ ] 完整测试覆盖

---

## 🎓 学习检查

完成任务后，回答以下问题：

1. **架构设计**: 
   - 为什么要分离链上和链下逻辑？
   - 如何保证系统的可靠性和性能？

2. **风险控制**:
   - 你实现了哪些风险控制机制？
   - 如何处理交易失败的情况？

3. **性能优化**:
   - 你采用了哪些优化技巧？
   - 如何减少延迟和提高吞吐量？

4. **实际应用**:
   - 这个机器人在真实环境中还需要什么改进？
   - 如何应对市场竞争和 MEV 竞赛？

---

## 💡 提示和最佳实践

### 开发提示
1. **先实现核心功能，再优化**: 先让系统跑起来，再进行性能优化
2. **充分测试**: 每个模块都要有单元测试，整体要有集成测试
3. **详细日志**: 记录所有关键操作，便于调试和分析
4. **错误处理**: 所有外部调用都要有错误处理和重试机制

### 调试技巧
1. 使用 `aptos move test --coverage` 查看测试覆盖率
2. 使用 `console.log` 和 `debug` 模块追踪执行流程
3. 在测试网上先小额测试，验证逻辑正确后再增加金额

### 安全注意事项
1. **私钥管理**: 永远不要将私钥硬编码，使用环境变量
2. **金额限制**: 设置单笔和每日最大交易金额
3. **滑点保护**: 始终设置合理的滑点保护
4. **超时控制**: 避免交易长时间挂起

---

## 🎯 扩展挑战（可选）

如果提前完成，可以尝试：

1. **多链支持**: 扩展到支持多条链的套利
2. **机器学习**: 使用历史数据训练模型，预测套利机会
3. **Gas 优化**: 研究批量交易，减少 Gas 成本
4. **MEV 保护**: 实现私有内存池提交，避免被抢跑

---

**完成后别忘了**:
- ✅ 运行所有测试
- ✅ 检查代码质量
- ✅ 更新文档
- ✅ 完成每日考试

祝你编码愉快！🚀
