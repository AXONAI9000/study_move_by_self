# 实践任务 - 构建借贷协议 MVP

## 🎯 任务概述

你的任务是构建一个完整的去中心化借贷协议 MVP，整合本周所学的所有知识。

**项目名称**：AptosLend  
**目标**：实现一个支持多资产存款、借款和清算的借贷协议  
**时间**：6-8 小时  

---

## 📋 功能需求

### 阶段 1：核心借贷功能（必需）⭐⭐⭐

#### 1.1 存款功能
```
功能：用户存入支持的资产
输入：资产类型、数量
输出：aToken 凭证
要求：
  - 验证资产类型被支持
  - 铸造等量 aToken
  - 更新储备金数据
  - 发射 DepositEvent
```

#### 1.2 取款功能
```
功能：用户赎回存款
输入：资产类型、数量
输出：底层资产
要求：
  - 验证用户有足够的 aToken
  - 销毁 aToken
  - 转移底层资产
  - 更新储备金数据
  - 发射 WithdrawEvent
```

#### 1.3 借款功能
```
功能：用户抵押借款
输入：借款资产、借款数量
输出：借出的资产
要求：
  - 验证抵押品充足
  - 计算健康因子
  - 检查借款能力
  - 转移借款资产
  - 更新用户债务
  - 发射 BorrowEvent
```

#### 1.4 还款功能
```
功能：用户偿还借款
输入：资产类型、还款数量
输出：减少债务
要求：
  - 计算实际债务（含利息）
  - 验证还款数量
  - 更新用户债务
  - 更新储备金
  - 发射 RepayEvent
```

### 阶段 2：利率和利息（必需）⭐⭐⭐

#### 2.1 利率模型
```
实现线性利率模型：
  - 计算资金使用率
  - 根据使用率计算借款利率
  - 计算存款利率
  - 支持参数配置

参数建议：
  base_rate: 0%
  slope1: 4%
  slope2: 75%
  optimal_utilization: 80%
```

#### 2.2 利息累积
```
功能：实时累积利息
要求：
  - 使用索引（index）机制
  - 支持线性或复利累积
  - 精确到秒级
  - Gas 高效
```

### 阶段 3：风险管理（必需）⭐⭐⭐

#### 3.1 健康因子计算
```
功能：计算账户健康度
公式：HF = (抵押品价值 × 清算阈值) / 借款价值
要求：
  - 支持多种抵押品
  - 实时更新
  - 精度处理
```

#### 3.2 清算机制
```
功能：清算不健康账户
触发条件：健康因子 < 1.0
要求：
  - 验证清算条件
  - 计算清算数量（最多 50%）
  - 计算清算奖励（如 5%）
  - 转移抵押品给清算人
  - 更新账户状态
  - 发射 LiquidationEvent
```

### 阶段 4：价格预言机（简化版可选）⭐

#### 4.1 价格接口
```
功能：提供资产价格
简化实现：
  - 使用固定价格（测试）
  - 或集成 Pyth/Switchboard
  
要求：
  - 统一的价格接口
  - 价格精度处理
  - 异常价格保护
```

---

## 🏗️ 技术要求

### 数据结构设计

#### LendingPool
```move
struct LendingPool has key {
    reserves: Table<String, Reserve>,
    user_data: Table<address, UserAccount>,
    supported_assets: vector<String>,
    admin: address,
}
```

#### Reserve
```move
struct Reserve has store {
    symbol: String,
    total_deposits: u64,
    total_borrows: u64,
    available_liquidity: u64,
    ltv: u64,
    liquidation_threshold: u64,
    liquidation_bonus: u64,
    reserve_factor: u64,
    last_update_timestamp: u64,
    liquidity_index: u128,
    borrow_index: u128,
}
```

#### UserAccount
```move
struct UserAccount has store {
    collateral: SimpleMap<String, u64>,
    borrows: SimpleMap<String, BorrowInfo>,
    health_factor: u128,
}
```

### 函数接口

#### 用户操作
```move
// 存款
public entry fun deposit<CoinType>(
    user: &signer,
    amount: u64
)

// 取款
public entry fun withdraw<CoinType>(
    user: &signer,
    amount: u64
)

// 借款
public entry fun borrow<CoinType>(
    user: &signer,
    amount: u64
)

// 还款
public entry fun repay<CoinType>(
    user: &signer,
    amount: u64
)

// 清算
public entry fun liquidate(
    liquidator: &signer,
    borrower: address,
    debt_asset: String,
    collateral_asset: String,
    debt_to_cover: u64
)
```

#### 查询函数
```move
// 查询用户存款
public fun get_user_deposits(user: address): SimpleMap<String, u64>

// 查询用户借款
public fun get_user_borrows(user: address): SimpleMap<String, u64>

// 查询健康因子
public fun get_health_factor(user: address): u128

// 查询借款能力
public fun get_borrowing_power(user: address): u64
```

---

## ✅ 验收标准

### 代码质量（30分）
- [ ] 代码结构清晰，模块化设计
- [ ] 变量命名规范，注释详细
- [ ] 错误处理完善
- [ ] 无编译警告

### 功能完整性（40分）
- [ ] 存款和取款功能正常
- [ ] 借款和还款功能正常
- [ ] 健康因子计算正确
- [ ] 清算机制正常工作
- [ ] 利息累积准确

### 测试覆盖（20分）
- [ ] 每个函数至少一个单元测试
- [ ] 边界条件测试
- [ ] 异常情况测试
- [ ] 集成测试

### 部署验证（10分）
- [ ] 成功编译
- [ ] 成功部署到测试网
- [ ] 功能验证通过

---

## 📝 任务步骤

### Step 1: 环境准备（30分钟）
```bash
# 创建新项目
aptos move init --name aptoslend

# 配置 Move.toml
[package]
name = "AptosLend"
version = "1.0.0"

[addresses]
lending_protocol = "_"

[dependencies]
AptosFramework = { git = "https://github.com/aptos-labs/aptos-core.git", subdir = "aptos-move/framework/aptos-framework/", rev = "main" }
```

### Step 2: 实现核心数据结构（1小时）
- [ ] 定义 LendingPool
- [ ] 定义 Reserve
- [ ] 定义 UserAccount
- [ ] 定义事件结构
- [ ] 定义错误常量

### Step 3: 实现存款和取款（1.5小时）
- [ ] deposit 函数
- [ ] withdraw 函数
- [ ] aToken 铸造/销毁逻辑
- [ ] 储备金更新逻辑
- [ ] 编写测试

### Step 4: 实现借款和还款（1.5小时）
- [ ] borrow 函数
- [ ] repay 函数
- [ ] 借款能力计算
- [ ] 健康因子更新
- [ ] 编写测试

### Step 5: 实现利率模型（1小时）
- [ ] 利率计算函数
- [ ] 索引更新逻辑
- [ ] 利息累积
- [ ] 编写测试

### Step 6: 实现清算机制（1小时）
- [ ] liquidate 函数
- [ ] 清算条件验证
- [ ] 清算奖励计算
- [ ] 编写测试

### Step 7: 集成测试（1小时）
- [ ] 完整借贷流程测试
- [ ] 多用户场景测试
- [ ] 边界条件测试
- [ ] 压力测试

### Step 8: 部署和验证（30分钟）
- [ ] 编译通过
- [ ] 部署到测试网
- [ ] 功能验证
- [ ] 编写部署文档

---

## 🧪 测试用例

### 基础功能测试

#### 测试 1: 存款和取款
```move
#[test(user = @0x123)]
fun test_deposit_and_withdraw(user: &signer) {
    // 初始化
    init_for_test();
    
    // 用户存入 100 APT
    deposit<APT>(user, 100);
    
    // 验证余额
    assert!(get_user_deposits(@0x123) == 100, 0);
    
    // 用户取出 50 APT
    withdraw<APT>(user, 50);
    
    // 验证余额
    assert!(get_user_deposits(@0x123) == 50, 0);
}
```

#### 测试 2: 借款和还款
```move
#[test(user = @0x123)]
fun test_borrow_and_repay(user: &signer) {
    // 初始化
    init_for_test();
    
    // 存入 100 APT 作为抵押品
    deposit<APT>(user, 100);
    
    // 借出 50 USDC
    borrow<USDC>(user, 50);
    
    // 验证借款
    assert!(get_user_borrows(@0x123) == 50, 0);
    
    // 还款 30 USDC
    repay<USDC>(user, 30);
    
    // 验证剩余借款
    assert!(get_user_borrows(@0x123) == 20, 0);
}
```

#### 测试 3: 健康因子和清算
```move
#[test(borrower = @0x123, liquidator = @0x456)]
fun test_liquidation(borrower: &signer, liquidator: &signer) {
    // 初始化
    init_for_test();
    
    // 借款人存入抵押品并借款
    deposit<APT>(borrower, 100);
    borrow<USDC>(borrower, 80);
    
    // 模拟价格下跌，使健康因子 < 1.0
    set_test_price("APT", 8); // APT 价格从 $10 降到 $8
    
    // 验证可以清算
    let hf = get_health_factor(@0x123);
    assert!(hf < HEALTH_FACTOR_LIQUIDATION_THRESHOLD, 0);
    
    // 清算人执行清算
    liquidate(liquidator, @0x123, b"USDC", b"APT", 40);
    
    // 验证清算后健康因子恢复
    let hf_after = get_health_factor(@0x123);
    assert!(hf_after > HEALTH_FACTOR_LIQUIDATION_THRESHOLD, 0);
}
```

### 边界测试

#### 测试 4: 超额借款失败
```move
#[test(user = @0x123)]
#[expected_failure(abort_code = E_INSUFFICIENT_COLLATERAL)]
fun test_over_borrow(user: &signer) {
    init_for_test();
    
    deposit<APT>(user, 100); // 抵押品价值 $1000
    borrow<USDC>(user, 900); // 尝试借出 $900 (超过 LTV)
}
```

#### 测试 5: 零金额操作失败
```move
#[test(user = @0x123)]
#[expected_failure(abort_code = E_AMOUNT_ZERO)]
fun test_zero_deposit(user: &signer) {
    init_for_test();
    deposit<APT>(user, 0);
}
```

---

## 💡 提示和建议

### 常见问题

**Q1: 如何处理精度问题？**
```move
// 使用 RAY（10^27）作为精度基准
const RAY: u128 = 1_000_000_000_000_000_000_000_000_000;

// 示例：计算带精度的乘法
fun ray_mul(a: u128, b: u128): u128 {
    (a * b) / RAY
}
```

**Q2: 如何避免除零错误？**
```move
public fun calculate_utilization(total_borrows: u64, available: u64): u64 {
    let total = total_borrows + available;
    if (total == 0) {
        return 0;
    };
    ((total_borrows as u128) * 10000 / (total as u128)) as u64
}
```

**Q3: 如何测试时间相关的逻辑？**
```move
#[test_only]
use aptos_framework::timestamp;

#[test]
fun test_interest_accrual() {
    // 设置初始时间
    timestamp::update_global_time_for_test(1000);
    
    // ... 执行操作 ...
    
    // 前进时间
    timestamp::update_global_time_for_test(2000);
    
    // 验证利息累积
}
```

### 开发技巧

1. **增量开发**
   - 先实现最简单的功能
   - 逐步添加复杂性
   - 每个阶段充分测试

2. **使用启动代码**
   - `启动代码/` 文件夹提供了框架
   - 填充 TODO 标记的部分
   - 可以修改结构以满足需求

3. **调试技巧**
   - 使用 `debug::print()` 输出调试信息
   - 编写小的单元测试验证逻辑
   - 使用 `assert!` 验证假设

4. **性能优化**
   - 批量更新而非频繁更新
   - 缓存计算结果
   - 使用高效的数据结构

---

## 📦 交付清单

完成任务后，请确保：

### 代码文件
- [ ] `lending_pool.move` - 核心借贷池
- [ ] `interest_rate.move` - 利率模型
- [ ] `health_factor.move` - 健康因子计算
- [ ] `liquidation.move` - 清算逻辑
- [ ] `errors.move` - 错误定义
- [ ] `events.move` - 事件定义

### 测试文件
- [ ] `lending_pool_tests.move` - 核心功能测试
- [ ] `interest_rate_tests.move` - 利率测试
- [ ] `liquidation_tests.move` - 清算测试
- [ ] `integration_tests.move` - 集成测试

### 文档文件
- [ ] `ARCHITECTURE.md` - 架构说明
- [ ] `API.md` - API 文档
- [ ] `DEPLOYMENT.md` - 部署说明
- [ ] `TEST_REPORT.md` - 测试报告

### 部署证明
- [ ] 测试网地址
- [ ] 交易哈希
- [ ] 功能验证截图

---

## 🎯 评分细则

### 功能实现（40分）
- 存款/取款（10分）
- 借款/还款（10分）
- 利率模型（10分）
- 清算机制（10分）

### 代码质量（25分）
- 架构设计（10分）
- 代码规范（8分）
- 错误处理（7分）

### 测试覆盖（20分）
- 单元测试（10分）
- 集成测试（5分）
- 边界测试（5分）

### 文档质量（10分）
- 架构文档（4分）
- API 文档（3分）
- 部署文档（3分）

### 部署验证（5分）
- 编译成功（2分）
- 部署成功（2分）
- 功能验证（1分）

**总分：100分**  
**及格线：70分**  
**优秀线：85分**

---

## 🚀 开始实现

1. 进入 `你的答案/` 文件夹
2. 从 `启动代码/` 复制模板
3. 开始编码！

**祝你成功！记住：一步一步来，先让基础功能工作！💪**
