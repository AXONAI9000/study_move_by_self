# NFT Token Standard 深入 - 核心概念

## 📚 目录

1. [NFT 基础概念](#1-nft-基础概念)
2. [Aptos Token Standard 演进](#2-aptos-token-standard-演进)
3. [对象模型详解](#3-对象模型详解)
4. [Collection 深度解析](#4-collection-深度解析)
5. [Token 对象结构](#5-token-对象结构)
6. [元数据与属性系统](#6-元数据与属性系统)
7. [NFT 生命周期管理](#7-nft-生命周期管理)
8. [实际案例分析](#8-实际案例分析)

---

## 1. NFT 基础概念

### 1.1 什么是 NFT？

NFT（Non-Fungible Token，非同质化代币）是一种**唯一**且**不可互换**的数字资产。

```
对比：同质化 vs 非同质化

同质化代币（FT）：
  - 每个代币完全相同
  - 可以互换（1 USDC = 另1 USDC）
  - 可分割（0.5 USDC）
  - 示例：BTC, ETH, USDC

非同质化代币（NFT）：
  - 每个代币独一无二
  - 不可互换（NFT #1 ≠ NFT #2）
  - 不可分割（最小单位是 1）
  - 示例：数字艺术、游戏道具、域名
```

### 1.2 NFT 的核心特性

```
1. 唯一性（Uniqueness）
   - 每个 NFT 有唯一标识符
   - 通过合约地址 + Token ID 确定
   
2. 所有权（Ownership）
   - 明确的所有者
   - 可验证、可转移
   
3. 不可分割性（Indivisibility）
   - 最小单位是 1
   - 不能分成 0.5 个
   
4. 可编程性（Programmability）
   - 可以附加逻辑和属性
   - 可以动态变化
```

### 1.3 NFT 的应用场景

```
1. 数字艺术
   - 艺术作品
   - 收藏品
   - 音乐、视频

2. 游戏道具
   - 装备、皮肤
   - 角色、宠物
   - 土地、建筑

3. 虚拟世界
   - 虚拟土地
   - 虚拟身份
   - 虚拟物品

4. 实用工具
   - 域名
   - 会员资格
   - 门票、证书

5. 金融应用
   - 贷款抵押品
   - 保险凭证
   - 债券、票据
```

---

## 2. Aptos Token Standard 演进

### 2.1 Token v1 vs Token v2

Aptos 有两个 Token 标准版本，v2 是完全重新设计的版本。

```
Token v1（Legacy）:
  架构：基于账户资源
  优点：简单直接
  缺点：
    - 灵活性差
    - 难以扩展
    - Gas 成本高
    - 不支持复杂逻辑

Token v2（Current）:
  架构：基于对象模型（Object Model）
  优点：
    - 高度可组合
    - 灵活可扩展
    - 支持复杂逻辑
    - 更好的 Gas 效率
  缺点：
    - 学习曲线陡峭
    - 需要理解对象模型
```

### 2.2 为什么选择对象模型？

```
传统账户资源模型的问题：

问题 1: 资源所有权
  - NFT 存储在用户账户中
  - 转移需要复杂的资源操作
  - 难以实现委托和租赁

问题 2: 可组合性
  - 资源间组合困难
  - 难以实现 NFT 嵌套
  - 扩展性受限

问题 3: Gas 成本
  - 每次操作都需要访问多个资源
  - 批量操作困难

对象模型的优势：

优势 1: 独立实体
  - 每个 NFT 是独立对象
  - 有自己的地址
  - 可以独立存在和移动

优势 2: 引用系统
  - 通过对象引用访问
  - 无需复制整个对象
  - 高效的批量操作

优势 3: 可组合性
  - 对象可以包含其他对象
  - 支持复杂的组合结构
  - 灵活的权限控制
```

### 2.3 对象模型核心概念

```
Object（对象）:
  - 区块链上的独立实体
  - 有唯一的对象地址
  - 可以拥有资源
  - 可以被引用和转移

ObjectRef（对象引用）:
  - 指向对象的引用
  - 不拥有对象，只是指针
  - 用于访问和操作对象

Object Owner（对象所有者）:
  - 拥有对象的账户或对象
  - 控制对象的转移
  - 可以是用户或智能合约
```

---

## 3. 对象模型详解

### 3.1 对象的创建

在 Aptos 中，创建对象有几种方式：

```move
use aptos_framework::object::{Self, Object, ConstructorRef};

// 方式 1: 创建命名对象（Named Object）
// 地址可预测，基于创建者地址和种子
public fun create_named_object(
    creator: &signer,
    seed: vector<u8>
): ConstructorRef {
    object::create_named_object(creator, seed)
}

// 方式 2: 创建普通对象（Regular Object）
// 地址随机生成，不可预测
public fun create_regular_object(
    creator: &signer
): ConstructorRef {
    object::create_object_from_account(creator)
}

// 方式 3: 创建粘性对象（Sticky Object）
// 对象无法被删除
public fun create_sticky_object(
    creator: &signer,
    seed: vector<u8>
): ConstructorRef {
    object::create_sticky_object(creator::address_of(creator), seed)
}
```

### 3.2 ConstructorRef 详解

```
ConstructorRef（构造引用）:
  - 对象创建时返回
  - 只在创建时有效
  - 用于初始化对象
  - 可以生成其他引用

可以生成的引用类型：

1. ExtendRef（扩展引用）
   - 可以生成 Signer
   - 用于对象内部操作
   
2. TransferRef（转移引用）
   - 控制对象转移权限
   - 可以禁用/启用转移
   
3. DeleteRef（删除引用）
   - 控制对象删除权限
   - 可以删除对象
   
4. MutatorRef（可变引用）
   - 允许修改对象的可变字段
```

### 3.3 对象的地址计算

```
命名对象地址计算：
  object_address = sha3_256(creator_address || seed || 0xFE)

示例：
  创建者: 0x123...
  种子: b"my_nft_collection"
  
  对象地址 = sha3_256(0x123... || b"my_nft_collection" || 0xFE)
            = 0xabc...（可预测）

好处：
  - 可以在创建前知道地址
  - 便于引用和查找
  - 适合 Collection 等固定对象
```

### 3.4 对象的所有权

```
对象所有权类型：

1. 用户所有
   Owner: 用户账户地址
   特点: 用户可以转移对象
   
2. 对象所有
   Owner: 另一个对象地址
   特点: 创建嵌套结构
   
3. 系统所有
   Owner: 系统地址（如 0x0）
   特点: 不可转移（如 Collection）

所有权转移：
  - 只有所有者可以转移
  - 可以转移给用户或对象
  - 转移是原子操作
```

---

## 4. Collection 深度解析

### 4.1 Collection 的本质

```
Collection 是什么？
  - NFT 的容器和分组单位
  - 一个独立的对象（Object）
  - 定义 NFT 的共同属性
  - 管理铸造权限和供应量

Collection 包含的信息：
  1. 基础信息
     - Name: Collection 名称
     - Description: 描述
     - URI: 元数据链接
     
  2. 创建者信息
     - Creator: 创建者地址
     - Creation Time: 创建时间
     
  3. 供应信息
     - Current Supply: 当前供应量
     - Max Supply: 最大供应量（可选）
     
  4. 版税信息
     - Royalty: 版税比例
     - Payee: 版税接收者
```

### 4.2 Collection 数据结构

```move
/// Collection 核心结构
struct Collection has key {
    /// Collection 的创建者
    creator: address,
    /// Collection 描述
    description: String,
    /// Collection 名称
    name: String,
    /// 元数据 URI
    uri: String,
    /// 可变描述引用（可选）
    mutator_ref: Option<MutatorRef>,
}

/// 固定供应量配置
struct FixedSupply has key {
    /// 当前铸造数量
    current_supply: u64,
    /// 最大供应量
    max_supply: u64,
}

/// 无限供应量标记
struct UnlimitedSupply has key {
    current_supply: u64,
}

/// 版税配置
struct Royalty has key {
    /// 版税分子
    numerator: u64,
    /// 版税分母（通常是 10000，表示基点）
    denominator: u64,
    /// 版税接收地址
    payee_address: address,
}
```

### 4.3 创建 Collection

```move
use aptos_token_objects::collection;

/// 创建固定供应量的 Collection
public entry fun create_fixed_collection(
    creator: &signer,
    description: String,
    name: String,
    uri: String,
    max_supply: u64,
    royalty_numerator: u64,    // 如 250 表示 2.5%
    royalty_denominator: u64   // 通常 10000
) {
    // 创建 Collection
    collection::create_fixed_collection(
        creator,
        description,
        max_supply,
        name,
        option::some(royalty_numerator, royalty_denominator),
        uri,
    );
}

/// 创建无限供应量的 Collection
public entry fun create_unlimited_collection(
    creator: &signer,
    description: String,
    name: String,
    uri: String,
) {
    collection::create_unlimited_collection(
        creator,
        description,
        name,
        option::none(),  // 无版税
        uri,
    );
}
```

### 4.4 Collection 权限管理

```
铸造权限：
  - 默认：只有创建者可以铸造
  - 扩展：可以委托给其他地址
  - 控制：通过 MintRef 控制

转移权限：
  - Collection 本身通常不可转移
  - 所有权固定为创建者或系统

修改权限：
  - 描述、URI 可以设置为可变
  - 需要保存 MutatorRef
  - 供应量、名称不可修改
```

### 4.5 Collection 元数据标准

```json
{
  "name": "Cool NFT Collection",
  "description": "A collection of cool NFTs",
  "image": "ipfs://QmXxx.../collection.png",
  "external_url": "https://example.com",
  "seller_fee_basis_points": 250,  // 2.5% 版税
  "fee_recipient": "0x123..."
}
```

---

## 5. Token 对象结构

### 5.1 Token 与 TokenData

在 Aptos Token v2 中，Token 由两部分组成：

```
TokenData（不可变元数据）:
  - 名称（Name）
  - 描述（Description）
  - URI（元数据链接）
  - 所属 Collection
  
Token（实际 NFT 实例）:
  - TokenData 引用
  - 所有者
  - Property Map（可变属性）
  - 唯一标识符
```

### 5.2 Token 数据结构

```move
/// Token 核心结构
struct Token has key {
    /// 所属 Collection
    collection: Object<Collection>,
    /// Token 索引（在 Collection 中的编号）
    index: u64,
    /// Token 描述
    description: String,
    /// Token 名称
    name: String,
    /// 元数据 URI
    uri: String,
    /// 可变引用（可选）
    mutator_ref: Option<MutatorRef>,
}

/// Property Map（可变属性）
struct PropertyMap has key {
    /// 属性映射
    inner: SimpleMap<String, PropertyValue>,
}

/// 属性值
struct PropertyValue has store {
    /// 值类型（如 u64, String, bool等）
    type: u8,
    /// 实际值（编码后的字节）
    value: vector<u8>,
}
```

### 5.3 Token 铸造流程

```move
use aptos_token_objects::token;

/// 铸造 NFT
public entry fun mint_nft(
    creator: &signer,
    collection_name: String,
    description: String,
    name: String,
    uri: String,
    property_keys: vector<String>,
    property_types: vector<String>,
    property_values: vector<vector<u8>>,
) {
    // 1. 创建 Token 对象
    let constructor_ref = token::create(
        creator,
        collection_name,
        description,
        name,
        option::none(),  // 无版税覆盖
        uri,
    );
    
    // 2. 添加属性
    let property_mutator_ref = property_map::generate_mutator_ref(&constructor_ref);
    property_map::init(&constructor_ref, property_mutator_ref);
    
    // 3. 设置属性值
    let i = 0;
    while (i < vector::length(&property_keys)) {
        let key = *vector::borrow(&property_keys, i);
        let type = *vector::borrow(&property_types, i);
        let value = *vector::borrow(&property_values, i);
        
        property_map::add(&property_mutator_ref, key, type, value);
        i = i + 1;
    };
}
```

### 5.4 Token ID 和地址

```
Token 唯一标识：
  - Object Address: 对象地址（全局唯一）
  - Collection + Index: Collection 内唯一
  
获取 Token：
  方式 1: 通过对象地址
    let token_obj = object::address_to_object<Token>(token_addr);
    
  方式 2: 通过 Collection + Name
    let token_addr = token::create_token_address(
        creator_addr,
        collection_name,
        token_name
    );
```

---

## 6. 元数据与属性系统

### 6.1 链上 vs 链下元数据

```
链上元数据（On-chain）:
  存储位置: 智能合约
  优点:
    - 永久存储
    - 可编程
    - 可验证
  缺点:
    - 成本高
    - 大小受限
  
  适用:
    - 核心属性（名称、稀有度）
    - 游戏数据
    - 动态属性

链下元数据（Off-chain）:
  存储位置: IPFS、Arweave 等
  优点:
    - 成本低
    - 大小不限
    - 支持富媒体
  缺点:
    - 可能丢失
    - 依赖外部服务
  
  适用:
    - 图片、视频
    - 详细描述
    - 静态数据
```

### 6.2 元数据标准格式

```json
{
  "name": "Cool NFT #1",
  "description": "This is a really cool NFT",
  "image": "ipfs://QmXxx.../image.png",
  "external_url": "https://example.com/nft/1",
  "attributes": [
    {
      "trait_type": "Background",
      "value": "Blue"
    },
    {
      "trait_type": "Rarity",
      "value": "Legendary",
      "display_type": "string"
    },
    {
      "trait_type": "Power",
      "value": 95,
      "display_type": "number",
      "max_value": 100
    },
    {
      "trait_type": "Generation",
      "value": 1,
      "display_type": "number"
    }
  ]
}
```

### 6.3 Property Map 详解

Property Map 是 Aptos 的链上动态属性系统：

```move
use aptos_token_objects::property_map;

/// 添加属性
public fun add_property(
    mutator_ref: &property_map::MutatorRef,
    key: String,
    type: String,
    value: vector<u8>
) {
    property_map::add(mutator_ref, key, type, value);
}

/// 更新属性
public fun update_property(
    mutator_ref: &property_map::MutatorRef,
    key: String,
    type: String,
    value: vector<u8>
) {
    property_map::update(mutator_ref, key, type, value);
}

/// 读取属性
public fun read_property<T: copy + drop>(
    object: Object<Token>,
    key: String
): T {
    property_map::read<T>(object, &key)
}

/// 移除属性
public fun remove_property(
    mutator_ref: &property_map::MutatorRef,
    key: String
) {
    property_map::remove(mutator_ref, &key);
}
```

### 6.4 支持的属性类型

```move
支持的类型：

1. 布尔值（bool）
   type: "bool"
   value: bcs::to_bytes(&true)

2. 整数（u8, u64, u128, u256）
   type: "u64"
   value: bcs::to_bytes(&100u64)

3. 字符串（String）
   type: "0x1::string::String"
   value: bcs::to_bytes(&string::utf8(b"Hello"))

4. 地址（address）
   type: "address"
   value: bcs::to_bytes(&@0x123)

5. 向量（vector）
   type: "vector<u64>"
   value: bcs::to_bytes(&vector[1, 2, 3])
```

### 6.5 动态属性应用场景

```
游戏道具：
  - Level: 等级（可升级）
  - HP: 生命值（可变化）
  - Equipment: 装备（可更换）
  
收藏品：
  - Rarity: 稀有度（可揭示）
  - Owner History: 所有权历史
  - Authenticity: 真伪验证
  
会员资格：
  - Tier: 等级（可升级）
  - Expiry: 过期时间
  - Benefits: 权益列表
```

---

## 7. NFT 生命周期管理

### 7.1 NFT 完整生命周期

```
1. 创建 Collection
   ↓
2. 铸造 Token
   ↓
3. 转移所有权
   ↓
4. 更新属性（可选）
   ↓
5. 销毁 Token（可选）
```

### 7.2 铸造（Minting）

```move
/// 基础铸造
public entry fun mint(
    creator: &signer,
    collection: String,
    description: String,
    name: String,
    uri: String,
) {
    token::create(
        creator,
        collection,
        description,
        name,
        option::none(),
        uri,
    );
}

/// 铸造并转移
public entry fun mint_and_transfer(
    creator: &signer,
    collection: String,
    description: String,
    name: String,
    uri: String,
    recipient: address,
) {
    let constructor_ref = token::create(
        creator,
        collection,
        description,
        name,
        option::none(),
        uri,
    );
    
    // 获取对象
    let object_signer = object::generate_signer(&constructor_ref);
    let transfer_ref = object::generate_transfer_ref(&constructor_ref);
    
    // 转移给接收者
    let linear_transfer_ref = object::generate_linear_transfer_ref(&transfer_ref);
    object::transfer_with_ref(linear_transfer_ref, recipient);
}
```

### 7.3 转移（Transfer）

```move
/// 直接转移（如果允许）
public entry fun transfer(
    owner: &signer,
    token: Object<Token>,
    to: address,
) {
    object::transfer(owner, token, to);
}

/// 使用 TransferRef 转移
public fun transfer_with_ref(
    transfer_ref: &LinearTransferRef,
    to: address,
) {
    object::transfer_with_ref(transfer_ref, to);
}

/// 禁用转移
public fun disable_transfer(
    transfer_ref: &TransferRef
) {
    object::disable_ungated_transfer(transfer_ref);
}

/// 启用转移
public fun enable_transfer(
    transfer_ref: &TransferRef
) {
    object::enable_ungated_transfer(transfer_ref);
}
```

### 7.4 销毁（Burning）

```move
/// 销毁 Token
public entry fun burn(
    owner: &signer,
    token: Object<Token>,
) acquires TokenRefs {
    // 验证所有权
    assert!(
        object::is_owner(token, signer::address_of(owner)),
        ERROR_NOT_OWNER
    );
    
    // 获取 DeleteRef
    let token_addr = object::object_address(&token);
    let refs = borrow_global<TokenRefs>(token_addr);
    
    // 销毁对象
    object::delete(refs.delete_ref);
}
```

### 7.5 冻结和解冻

```move
/// 冻结转移
struct FrozenToken has key {
    transfer_ref: TransferRef,
}

public entry fun freeze_transfer(
    creator: &signer,
    token: Object<Token>,
) acquires FrozenToken {
    let frozen = borrow_global_mut<FrozenToken>(
        object::object_address(&token)
    );
    object::disable_ungated_transfer(&frozen.transfer_ref);
}

public entry fun unfreeze_transfer(
    creator: &signer,
    token: Object<Token>,
) acquires FrozenToken {
    let frozen = borrow_global_mut<FrozenToken>(
        object::object_address(&token)
    );
    object::enable_ungated_transfer(&frozen.transfer_ref);
}
```

---

## 8. 实际案例分析

### 8.1 案例：数字艺术 NFT

```
项目：Aptos Monkeys

Collection 设计：
  - Name: "Aptos Monkeys"
  - Max Supply: 10,000
  - Royalty: 5%
  
Token 属性：
  - Background: 颜色
  - Fur: 毛发类型
  - Eyes: 眼睛样式
  - Mouth: 嘴巴样式
  - Accessories: 配饰
  - Rarity Score: 稀有度评分

元数据：
  - 图片: IPFS 存储
  - 属性: 链上 Property Map
  - 描述: 链下 JSON
```

### 8.2 案例：游戏道具 NFT

```
项目：NFT RPG Game

Collection 设计：
  - Name: "Epic Weapons"
  - Max Supply: Unlimited
  - Royalty: 2.5%

Token 动态属性：
  链上属性（Property Map）:
    - weapon_type: "Sword"
    - level: 1 (可升级)
    - attack_power: 50 (可升级)
    - durability: 100 (会损耗)
    - owner_history: [address1, address2]
  
  链下属性（JSON）:
    - 3D 模型
    - 音效
    - 特效动画

升级逻辑：
  - 消耗游戏代币
  - 更新 level 属性
  - 计算新的 attack_power
  - 记录升级历史
```

### 8.3 案例：会员卡 NFT

```
项目：VIP Membership Card

Collection 设计：
  - Name: "Platinum Members"
  - Max Supply: 1,000
  - Royalty: 0% (实用型)

Token 属性：
  静态属性：
    - card_number: 唯一编号
    - issue_date: 发行日期
    - member_name: 会员姓名
  
  动态属性：
    - tier: "Platinum" (可升级到 Diamond)
    - points: 积分 (可累积)
    - expiry_date: 过期时间 (可续期)
    - benefits: 权益列表 (可更新)

应用场景：
  - 访问控制（验证 NFT 所有权）
  - 积分系统（更新 points）
  - 等级升级（更新 tier）
  - 权益管理（更新 benefits）
```

### 8.4 主流 NFT 项目对比

```
Ethereum (ERC-721) vs Aptos (Token v2)

架构：
  ERC-721: 合约内映射（tokenId → owner）
  Aptos v2: 独立对象（每个 NFT 是对象）

元数据：
  ERC-721: tokenURI() 返回 JSON URL
  Aptos v2: URI + Property Map 组合

转移：
  ERC-721: transferFrom()
  Aptos v2: object::transfer()

扩展性：
  ERC-721: 继承和接口
  Aptos v2: 对象组合和引用

Gas 成本：
  ERC-721: 较高（状态变更）
  Aptos v2: 较低（对象模型优化）
```

---

## 📊 总结对比

### Token v1 vs Token v2

| 特性 | Token v1 | Token v2 |
|------|----------|----------|
| 架构 | 账户资源 | 对象模型 |
| 灵活性 | 低 | 高 |
| 可组合性 | 差 | 优秀 |
| Gas 成本 | 高 | 低 |
| 学习曲线 | 平缓 | 陡峭 |
| 推荐使用 | ❌ 已弃用 | ✅ 当前标准 |

### 元数据存储选择

| 存储方式 | 成本 | 永久性 | 大小限制 | 适用场景 |
|---------|------|--------|---------|---------|
| 链上（Contract） | 高 | 最高 | 严格 | 核心属性 |
| IPFS | 低 | 高 | 无 | 图片、JSON |
| Arweave | 中 | 最高 | 无 | 重要数据 |
| 中心化服务器 | 最低 | 低 | 无 | 临时数据 |

---

## 🎯 关键要点

### Collection 设计

1. **命名策略**: 独特、易记、符合品牌
2. **供应量**: 稀缺性 vs 可及性
3. **版税设置**: 2.5%-10% 是常见范围
4. **元数据**: IPFS + 链上属性组合

### Token 创建

1. **唯一性**: 确保每个 Token 独一无二
2. **属性设计**: 静态 vs 动态，链上 vs 链下
3. **转移控制**: 是否允许自由转移
4. **可销毁性**: 是否支持销毁

### 最佳实践

1. **安全第一**: 严格的权限控制
2. **Gas 优化**: 批量操作，减少链上存储
3. **标准遵循**: 兼容市场和钱包
4. **用户体验**: 简化铸造和转移流程

---

## 🔗 扩展阅读

- [Aptos Token Standard 官方文档](https://aptos.dev/standards/digital-asset/)
- [ERC-721 标准](https://eips.ethereum.org/EIPS/eip-721)
- [OpenSea 元数据标准](https://docs.opensea.io/docs/metadata-standards)
- [IPFS 文档](https://docs.ipfs.tech/)

---

**恭喜你完成理论学习！现在让我们进入代码实现部分。** 🚀
