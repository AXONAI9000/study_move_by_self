# Day 24: NFT 批量操作与 Gas 优化 - 实践任务

## 📋 任务概览

今天的实践任务将让你实现一个**完整的批量 NFT 系统**，包括批量铸造、批量转移和 Gas 优化。

### 🎯 任务目标

- ✅ 实现批量铸造功能
- ✅ 实现批量转移功能
- ✅ 应用 Gas 优化技巧
- ✅ 实现元数据管理
- ✅ 编写完整测试

### ⏰ 预计时间

4.5 小时

---

## 📝 任务列表

### 任务 1: 批量铸造系统（必做）⭐⭐⭐⭐⭐

**难度**: ⭐⭐⭐⭐☆  
**预计时间**: 2 小时  
**分值**: 40 分

#### 任务描述

实现一个支持批量铸造的 NFT 系统，要求：

1. **基础功能**
   - 支持一次铸造多个 NFT
   - 支持分发给不同的接收者
   - 自动生成 Token ID
   - 记录铸造统计

2. **Gas 优化**
   - 批量铸造比单个铸造至少节省 40% Gas
   - 使用模板化元数据
   - 批量发射事件
   - 预计算常量

3. **安全要求**
   - 验证批次大小限制
   - 检查供应量上限
   - 权限控制
   - 防止重复铸造

#### 实现要求

```move
/// 你的实现应包含以下函数：

/// 1. 创建 Collection
public entry fun create_batch_collection(
    creator: &signer,
    name: String,
    description: String,
    max_supply: u64,
    base_uri: String,
)

/// 2. 批量铸造
public entry fun batch_mint(
    creator: &signer,
    collection_name: String,
    recipients: vector<address>,
    count_per_recipient: u64,
)

/// 3. 获取统计信息
public fun get_mint_stats(collection_addr: address): (u64, u64)
```

#### 验收标准

- [ ] 能够批量铸造至少 20 个 NFT
- [ ] Gas 成本比单个铸造节省 ≥ 40%
- [ ] 所有 NFT 正确分发给接收者
- [ ] 统计数据准确
- [ ] 通过所有边界测试

#### 提示

1. 使用 `vector` 遍历接收者
2. 预先计算 `collection_addr` 和 `base_uri`
3. 只发射一个批量事件
4. 使用位压缩存储属性

---

### 任务 2: 批量转移功能（必做）⭐⭐⭐⭐⭐

**难度**: ⭐⭐⭐☆☆  
**预计时间**: 1.5 小时  
**分值**: 30 分

#### 任务描述

实现批量转移 NFT 的功能，支持多种转移模式：

1. **一对一转移**: 多个 NFT 分别转给不同接收者
2. **一对多转移**: 多个 NFT 转给同一接收者
3. **批量所有权验证**: 一次性验证所有 NFT 的所有权

#### 实现要求

```move
/// 批量转移（一对一）
public entry fun batch_transfer_one_to_one(
    sender: &signer,
    token_addresses: vector<address>,
    recipients: vector<address>,
)

/// 批量转移（一对多）
public entry fun batch_transfer_one_to_many(
    sender: &signer,
    token_addresses: vector<address>,
    recipient: address,
)

/// 批量验证所有权
public fun verify_batch_ownership(
    owner: address,
    token_addresses: vector<address>,
): bool
```

#### 验收标准

- [ ] 支持批量转移至少 10 个 NFT
- [ ] 原子性：全部成功或全部失败
- [ ] 正确验证所有权
- [ ] 发射批量转移事件
- [ ] 处理错误情况

#### Gas 优化目标

- 批量转移 10 个 NFT 的 Gas < 单个转移 10 次的 50%

---

### 任务 3: 元数据优化系统（必做）⭐⭐⭐⭐

**难度**: ⭐⭐⭐⭐☆  
**预计时间**: 1 小时  
**分值**: 20 分

#### 任务描述

实现优化的元数据管理系统：

1. **模板化 URI**: 使用基础 URI + Token ID + 后缀
2. **属性打包**: 将多个属性压缩到一个整数
3. **链下存储**: 大数据存储在 IPFS，链上只存哈希

#### 实现要求

```move
/// 设置基础 URI
public entry fun set_base_uri(
    creator: &signer,
    collection_addr: address,
    base_uri: String,
)

/// 获取 Token URI（动态生成）
public fun get_token_uri(token_addr: address): String

/// 打包属性
public fun pack_attributes(
    rarity: u8,
    level: u8,
    category: u8,
): u32

/// 获取属性
public fun get_attribute(
    token_addr: address,
    attribute_type: u8,  // 0: rarity, 1: level, 2: category
): u8
```

#### 验收标准

- [ ] URI 动态生成，不存储完整 URI
- [ ] 属性正确打包和解包
- [ ] 每个 NFT 存储 < 100 bytes
- [ ] 属性读取功能正常

#### 存储优化目标

- 每个 NFT 存储大小 ≤ 64 bytes
- 相比存储完整元数据节省 ≥ 80%

---

### 任务 4: Gas 分析工具（选做）⭐⭐⭐

**难度**: ⭐⭐☆☆☆  
**预计时间**: 30 分钟  
**分值**: 10 分（加分项）

#### 任务描述

实现 Gas 使用分析和统计功能。

#### 实现要求

```move
/// 记录操作的 Gas 使用
struct GasRecord has store {
    operation_type: String,
    batch_size: u64,
    gas_used: u64,
    timestamp: u64,
}

/// 添加 Gas 记录
public entry fun record_gas_usage(
    account: &signer,
    operation: String,
    batch_size: u64,
    gas_used: u64,
)

/// 计算节省的 Gas
public fun calculate_savings(
    individual_gas: u64,
    batch_gas: u64,
): (u64, u64)  // (saved_gas, savings_percent)
```

---

## 🧪 测试要求

### 单元测试（必须通过）

创建测试文件 `tests/batch_operations_test.move`：

```move
#[test_only]
module nft_batch_ops::batch_operations_test {
    use nft_batch_ops::your_module;
    
    #[test(creator = @0x123, user1 = @0x456, user2 = @0x789)]
    fun test_batch_mint_success(
        creator: &signer,
        user1: address,
        user2: address,
    ) {
        // 测试批量铸造
        // 1. 创建 Collection
        // 2. 批量铸造给多个用户
        // 3. 验证每个用户收到 NFT
        // 4. 检查统计数据
    }
    
    #[test(sender = @0x123, receiver = @0x456)]
    fun test_batch_transfer_success(
        sender: &signer,
        receiver: address,
    ) {
        // 测试批量转移
        // 1. 铸造多个 NFT
        // 2. 批量转移
        // 3. 验证所有权变更
    }
    
    #[test(creator = @0x123)]
    #[expected_failure(abort_code = ERROR_BATCH_TOO_LARGE)]
    fun test_batch_size_limit(creator: &signer) {
        // 测试批次大小限制
        // 尝试铸造超过最大批次
        // 应该失败
    }
    
    #[test]
    fun test_pack_unpack_attributes() {
        // 测试属性打包和解包
        // 1. 打包属性
        // 2. 解包验证
        // 3. 确保数据一致
    }
}
```

### 性能测试

```move
#[test(creator = @0x123)]
fun test_gas_optimization(creator: &signer) {
    // 对比测试
    // 1. 单个铸造 10 次的 Gas
    // 2. 批量铸造 10 个的 Gas
    // 3. 计算节省百分比
    // 4. 验证 ≥ 40% 节省
}
```

### 边界测试

- 空批次（batch_size = 0）
- 单个元素（batch_size = 1）
- 最大批次（batch_size = MAX）
- 超过最大批次
- 无效地址
- 重复地址

---

## 📊 评分标准

### 功能完整性（60 分）

- 任务 1: 批量铸造系统 - 40 分
- 任务 2: 批量转移功能 - 30 分
- 任务 3: 元数据优化系统 - 20 分
- 任务 4: Gas 分析工具 - 10 分（加分）

### 代码质量（20 分）

- 代码规范：5 分
- 注释完整：5 分
- 错误处理：5 分
- 安全性：5 分

### 测试覆盖（20 分）

- 单元测试：10 分
- 边界测试：5 分
- 性能测试：5 分

### 总分

**基础分**: 100 分  
**加分项**: 10 分  
**满分**: 110 分

**及格线**: 70 分

---

## 💡 实现建议

### 开发顺序

1. **第一步**: 创建基础 Collection 结构
   ```move
   struct BatchCollection has key {
       minted_count: u64,
       max_supply: u64,
       base_uri: String,
   }
   ```

2. **第二步**: 实现单个 NFT 铸造
   - 先实现基础功能
   - 确保单个铸造正常工作

3. **第三步**: 扩展到批量铸造
   - 添加循环处理
   - 实现批量验证
   - 优化 Gas 消耗

4. **第四步**: 实现批量转移
   - 复用铸造经验
   - 添加所有权验证

5. **第五步**: 优化元数据
   - 实现 URI 模板
   - 添加属性打包

6. **第六步**: 编写测试
   - 覆盖所有功能
   - 测试边界情况

### 常见问题解决

#### 问题 1: 如何限制批次大小？

```move
const MAX_BATCH_SIZE: u64 = 50;

public entry fun batch_mint(..., count: u64, ...) {
    assert!(count <= MAX_BATCH_SIZE, ERROR_BATCH_TOO_LARGE);
    // ...
}
```

#### 问题 2: 如何优化 URI 存储？

```move
// ❌ 低效：每个 Token 存储完整 URI
struct Token {
    uri: String  // "ipfs://Qm.../12345.json"
}

// ✅ 高效：只存储 ID，动态组合
struct Collection {
    base_uri: String  // "ipfs://Qm.../"
}

struct Token {
    id: u64  // 12345
}

fun get_uri(token_id: u64): String {
    base_uri + to_string(token_id) + ".json"
}
```

#### 问题 3: 如何实现属性打包？

```move
// 打包三个 u8 到一个 u32
fun pack(a: u8, b: u8, c: u8): u32 {
    ((a as u32) << 0) | 
    ((b as u32) << 8) | 
    ((c as u32) << 16)
}

// 解包
fun unpack_a(packed: u32): u8 {
    ((packed >> 0) & 0xFF) as u8
}
```

#### 问题 4: 如何批量验证所有权？

```move
fun batch_verify_ownership(
    owner: address,
    tokens: &vector<address>
) {
    let i = 0;
    let len = vector::length(tokens);
    while (i < len) {
        let token_addr = *vector::borrow(tokens, i);
        let token_obj = object::address_to_object(token_addr);
        assert!(object::owner(token_obj) == owner, ERROR_NOT_OWNER);
        i = i + 1;
    };
}
```

---

## 🎯 优化目标

### Gas 优化目标

```
批量铸造（10 个 NFT）:
  - 单个铸造 10 次: ~10,000 Gas
  - 批量铸造 10 个: ~6,000 Gas
  - 节省: 40%

批量转移（10 个 NFT）:
  - 单个转移 10 次: ~8,000 Gas
  - 批量转移 10 个: ~4,000 Gas
  - 节省: 50%

元数据存储（每个 NFT）:
  - 完整存储: ~400 bytes
  - 优化存储: ~60 bytes
  - 节省: 85%
```

### 性能基准

```
操作吞吐量:
  - 批量铸造: ≥ 20 NFT/秒
  - 批量转移: ≥ 30 NFT/秒

响应时间:
  - 批量操作: < 3 秒
  - 查询操作: < 100 ms

成功率:
  - 正常批次: 100%
  - 边界情况: 正确处理
```

---

## 📦 提交要求

### 文件结构

```
sources/
├── batch_nft.move           # 主模块
├── metadata.move            # 元数据管理
└── utils.move               # 辅助函数

tests/
├── batch_mint_test.move     # 批量铸造测试
├── batch_transfer_test.move # 批量转移测试
├── metadata_test.move       # 元数据测试
└── gas_test.move           # Gas 优化测试

scripts/
├── deploy.move             # 部署脚本
└── demo.move               # 演示脚本

docs/
├── IMPLEMENTATION.md       # 实现说明
├── GAS_ANALYSIS.md        # Gas 分析报告
└── TEST_REPORT.md         # 测试报告
```

### 提交清单

- [ ] 所有源代码文件
- [ ] 完整的测试文件
- [ ] 部署和演示脚本
- [ ] 实现文档
- [ ] Gas 分析报告
- [ ] 测试报告
- [ ] README.md

---

## 🏆 挑战任务（进阶）

### 挑战 1: 动态批次优化

根据网络状况自动调整批次大小。

### 挑战 2: 并行批量铸造

利用 Aptos 的并行执行特性优化批量操作。

### 挑战 3: 批量铸造市场

实现一个支持批量购买 NFT 的市场合约。

### 挑战 4: Gas 代付功能

实现批量铸造时由项目方代付 Gas 的功能。

---

## 📞 获取帮助

### 参考资料

1. 代码示例：`01_理论学习/代码示例.move`
2. 核心概念：`01_理论学习/核心概念.md`
3. Aptos 文档：https://aptos.dev

### 常见错误

1. **批次过大导致 Gas 超限**
   - 解决：减小批次或优化代码

2. **属性打包错误**
   - 解决：检查位运算逻辑

3. **所有权验证失败**
   - 解决：确保 Token 对象类型正确

4. **URI 生成错误**
   - 解决：检查字符串拼接逻辑

---

## 🎓 学习成果

完成本任务后，你将：

✅ 掌握批量操作的设计和实现  
✅ 理解 Gas 优化的核心技巧  
✅ 能够优化大规模 NFT 项目  
✅ 具备实际项目开发能力

---

**准备好开始了吗？祝你实现顺利！💪**

*记住：先实现功能，再优化性能！*
