# Day 10 每日考试 - 选择题

## 说明
- 共 10 道选择题，每题 5 分，共 50 分
- 单选题，每题只有一个正确答案
- 答案请写在题目后面，格式：`答案：X`
- 完成后对照答案解析自我评分

---

## 第 1 题（5 分）

Move Prover 的主要作用是什么？

A. 编译 Move 代码为字节码  
B. 运行 Move 测试用例  
C. 使用数学方法证明代码满足规约  
D. 格式化 Move 代码

**答案：**

---

## 第 2 题（5 分）

以下哪个关键字用于定义函数的前置条件？

A. `ensures`  
B. `requires`  
C. `aborts_if`  
D. `invariant`

**答案：**

---

## 第 3 题（5 分）

在规约中，`old(x)` 表达式的含义是什么？

A. x 的旧值（函数执行前）  
B. x 的类型  
C. x 是否存在  
D. x 的默认值

**答案：**

---

## 第 4 题（5 分）

以下哪个规约正确描述了加法函数不会溢出？

```move
public fun add(a: u64, b: u64): u64 {
    a + b
}
```

A. `ensures result < 18446744073709551615`  
B. `requires a + b <= 18446744073709551615`  
C. `aborts_if a + b > 0`  
D. `invariant a > 0`

**答案：**

---

## 第 5 题（5 分）

以下关于 `aborts_if` 的说法，哪个是正确的？

A. 指定函数成功执行的条件  
B. 指定函数返回值的条件  
C. 指定函数会 abort 的条件  
D. 指定函数的循环条件

**答案：**

---

## 第 6 题（5 分）

在规约中，如何访问地址 `addr` 的全局资源 `Account`？

A. `Account(addr)`  
B. `global<Account>(addr)`  
C. `get<Account>(addr)`  
D. `borrow<Account>(addr)`

**答案：**

---

## 第 7 题（5 分）

以下哪个是数据不变量（invariant）的正确用途？

A. 定义函数的返回值  
B. 定义数据结构必须保持的性质  
C. 定义函数的参数类型  
D. 定义模块的可见性

**答案：**

---

## 第 8 题（5 分）

对于以下转账函数，哪个规约证明了总量守恒？

```move
public fun transfer(from: &mut Account, to: &mut Account, amount: u64) {
    from.balance = from.balance - amount;
    to.balance = to.balance + amount;
}
```

A. `ensures from.balance < old(from.balance)`  
B. `ensures to.balance > old(to.balance)`  
C. `ensures from.balance + to.balance == old(from.balance + to.balance)`  
D. `ensures amount > 0`

**答案：**

---

## 第 9 题（5 分）

以下关于形式化验证和测试的比较，哪个是错误的？

A. 形式化验证可以覆盖所有输入情况  
B. 测试只能验证特定的输入  
C. 形式化验证完全可以替代测试  
D. 形式化验证提供数学证明

**答案：**

---

## 第 10 题（5 分）

以下哪个量词表达式是正确的？（假设检查所有账户余额非负）

A. `invariant global<Account>(addr).balance >= 0`  
B. `invariant forall addr: address: global<Account>(addr).balance >= 0`  
C. `invariant forall addr: address where exists<Account>(addr): global<Account>(addr).balance >= 0`  
D. `invariant exists addr: address: global<Account>(addr).balance >= 0`

**答案：**

---

## 评分标准

- 9-10 题正确：优秀（90-100 分）- 完全掌握
- 7-8 题正确：良好（70-89 分）- 基本掌握
- 5-6 题正确：及格（60-69 分）- 需要复习
- 5 题以下：不及格（<60 分）- 需要重学

---

**完成后请查看答案解析进行自我评分！**
