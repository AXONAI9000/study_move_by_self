# Day 34 实践任务：Gas 优化实战

## 📋 任务概述

今天的实践任务将带你深入掌握 Gas 优化技巧。你将实现三个优化项目，涵盖存储优化、计算优化和批量操作，并使用工具测量优化效果。

**总时长**：4.5 小时

---

## 🎯 任务目标

完成今天的任务后，你将能够：

1. ✅ 实现高效的数据打包存储
2. ✅ 优化循环和条件分支
3. ✅ 实现批量操作接口
4. ✅ 使用 Gas Profiler 分析性能
5. ✅ 对比优化前后的 Gas 消耗
6. ✅ 生成 Gas 优化报告

---

## 📝 任务列表

### 任务 1: 存储优化实战 (1.5 小时)

#### 背景
你需要为一个 NFT 市场实现用户配置存储。每个用户有多个配置项，需要频繁读写。

#### 需求

**1.1 实现打包存储结构**

设计一个 `PackedUserConfig` 结构体，将以下字段打包到一个 `u256`：

```
字段列表：
- is_verified: bool (1 bit)
- is_premium: bool (1 bit)
- notification_enabled: bool (1 bit)
- auto_renew: bool (1 bit)
- tier: u8 (8 bits) - 支持 0-255 个级别
- referral_code: u16 (16 bits)
- max_listings: u8 (8 bits)
- discount_rate: u8 (8 bits) - 0-100%
- last_active_day: u16 (16 bits) - 天数偏移
- flags: u32 (32 bits) - 自定义标志位
- reserved: 剩余位保留
```

**要求**：
- 实现创建、读取、更新函数
- 提供位操作辅助函数
- 添加单元测试

**1.2 数据结构选择**

实现三个版本的用户列表：

```move
// Version 1: 使用 vector (小数据量)
struct UserListV1 {
    users: vector<address>,
}

// Version 2: 使用 SimpleMap (中等数据量)
struct UserListV2 {
    users: SimpleMap<address, PackedUserConfig>,
}

// Version 3: 使用 Table (大数据量)
struct UserListV3 {
    users: Table<address, PackedUserConfig>,
}
```

**要求**：
- 实现添加、查找、删除操作
- 编写性能测试对比
- 记录不同数据量下的 Gas 消耗

**1.3 延迟加载**

将用户数据分离为：
- 基础信息（高频访问）
- 详细配置（低频访问）
- 统计数据（低频访问）

**提交内容**：
```
sources/packed_storage.move       # 打包存储实现
tests/storage_benchmark.move      # 性能测试
docs/storage_optimization.md      # 优化报告
```

---

### 任务 2: 计算优化实战 (1.5 小时)

#### 背景
实现一个 DEX 路由器，需要在多个交易对中找到最优路径。

#### 需求

**2.1 循环优化**

实现一个价格聚合器，需要从多个 DEX 获取价格：

```move
// 待优化函数
public fun find_best_price(
    token_in: address,
    token_out: address,
    dexes: vector<address>
): (address, u64) {
    // 遍历所有 DEX，找到最优价格
    // 优化点：
    // 1. 缓存 vector::length
    // 2. 避免重复读取
    // 3. 提前退出条件
}
```

**要求**：
- 实现未优化版本
- 实现优化版本
- 对比 Gas 消耗
- 说明优化技巧

**2.2 条件分支优化**

实现手续费计算器，根据用户等级计算手续费：

```move
// 手续费阶梯：
// Bronze (80%用户): 1%
// Silver (15%用户): 0.75%
// Gold (4%用户): 0.5%
// Platinum (0.9%用户): 0.25%
// Diamond (0.1%用户): 0.1%
```

**要求**：
- 按概率排序条件
- 使用查找表优化
- 对比多重 if vs 查找表的性能

**2.3 数学运算优化**

实现利息计算函数：

```move
// 复利计算：A = P(1 + r/n)^(nt)
// 优化点：
// 1. 使用位运算代替乘除（适当时）
// 2. 合并多个除法
// 3. 避免重复计算
```

**提交内容**：
```
sources/compute_optimization.move # 计算优化实现
tests/compute_benchmark.move      # 性能测试
docs/compute_optimization.md      # 优化报告
```

---

### 任务 3: 批量操作与 Gas 分析工具 (1.5 小时)

#### 背景
构建一个完整的批量操作系统和 Gas 分析工具。

#### 需求

**3.1 实现批量操作**

实现以下批量接口：

```move
// 批量转账
public entry fun batch_transfer(
    from: &signer,
    recipients: vector<address>,
    amounts: vector<u64>
)

// 批量 Mint NFT
public entry fun batch_mint_nft(
    creator: &signer,
    recipients: vector<address>,
    token_uris: vector<String>
)

// 批量更新状态
public entry fun batch_update_status(
    admin: &signer,
    users: vector<address>,
    statuses: vector<u8>
)
```

**要求**：
- 实现参数验证
- 设置合理的批次上限
- 处理部分失败
- 添加 Gas 限制检查

**3.2 构建 Gas Profiler**

实现一个 Gas 分析模块：

```move
module gas_optimization::profiler {
    struct GasReport {
        function_name: String,
        gas_used: u64,
        iterations: u64,
        avg_gas_per_iteration: u64,
    }
    
    public fun profile_function<T>(
        name: String,
        iterations: u64,
        func: |u64| -> T
    ): GasReport
    
    public fun compare_implementations(
        name1: String,
        func1: ||,
        name2: String,
        func2: ||
    ): ComparisonReport
}
```

**要求**：
- 记录函数 Gas 消耗
- 支持多次运行取平均
- 生成对比报告
- 可视化结果

**3.3 性能基准测试**

为所有优化函数创建基准测试：

```move
#[test]
public fun benchmark_storage_operations() {
    // 测试不同数据结构的性能
    test_vector_operations(100);
    test_simple_map_operations(100);
    test_table_operations(100);
    
    // 记录和对比结果
}

#[test]
public fun benchmark_batch_operations() {
    // 对比单个 vs 批量操作
    test_single_transfer(100);
    test_batch_transfer(100);
    
    // 计算节省百分比
}
```

**提交内容**：
```
sources/batch_operations.move     # 批量操作实现
sources/gas_profiler.move         # Gas 分析工具
tests/benchmark_suite.move        # 完整基准测试
scripts/analyze_gas.ts            # Gas 分析脚本
docs/gas_optimization_report.md   # 综合优化报告
```

---

## 🔍 测试要求

### 功能测试

为每个优化函数编写测试：

```move
#[test]
public fun test_packed_storage() {
    // 测试数据打包和解包
    let config = create_packed_config(/* ... */);
    assert!(get_tier(&config) == expected_tier, 0);
    // ...
}

#[test]
public fun test_batch_transfer() {
    // 测试批量转账
    let recipients = vector[addr1, addr2, addr3];
    let amounts = vector[100, 200, 300];
    batch_transfer(&sender, recipients, amounts);
    // 验证结果
}
```

### 性能测试

对比优化前后的性能：

```move
#[test]
public fun compare_storage_performance() {
    let unoptimized_gas = measure_gas(|| {
        // 未优化的实现
    });
    
    let optimized_gas = measure_gas(|| {
        // 优化后的实现
    });
    
    let savings = unoptimized_gas - optimized_gas;
    let savings_percent = (savings * 100) / unoptimized_gas;
    
    // 记录结果
    print(&format!("Gas savings: {}%", savings_percent));
}
```

### Gas 基准测试

使用 Aptos CLI 进行 Gas 分析：

```bash
# 运行 Gas Profiler
aptos move test --gas-profiler

# 保存基准结果
aptos move test --gas-profiler > baseline_gas.txt

# 优化后再次测试
aptos move test --gas-profiler > optimized_gas.txt

# 对比结果
diff baseline_gas.txt optimized_gas.txt
```

---

## 📊 评分标准

### 任务 1: 存储优化 (40 分)

- [ ] 正确实现数据打包 (15 分)
  - 位操作正确性
  - 所有字段可访问
  - 边界情况处理

- [ ] 数据结构选择合理 (15 分)
  - 三种实现完整
  - 性能测试完善
  - 选择建议合理

- [ ] 延迟加载实现 (10 分)
  - 数据分离合理
  - 按需加载正确
  - 性能提升明显

### 任务 2: 计算优化 (30 分)

- [ ] 循环优化 (10 分)
  - 优化技巧正确
  - Gas 节省明显
  - 代码清晰

- [ ] 条件分支优化 (10 分)
  - 按概率排序
  - 查找表实现
  - 性能对比

- [ ] 数学运算优化 (10 分)
  - 位运算正确
  - 计算合并有效
  - 准确性保证

### 任务 3: 批量操作与工具 (30 分)

- [ ] 批量操作实现 (15 分)
  - 三个批量接口完整
  - 错误处理完善
  - Gas 节省显著

- [ ] Gas Profiler (10 分)
  - 功能完整
  - 数据准确
  - 报告清晰

- [ ] 基准测试 (5 分)
  - 测试覆盖全面
  - 对比有说服力
  - 文档完善

### 代码质量 (额外加分)

- [ ] 代码风格一致 (+3 分)
- [ ] 注释清晰完整 (+3 分)
- [ ] 测试覆盖率高 (+2 分)
- [ ] 文档专业详细 (+2 分)

**满分**：100 分 + 10 分加分

---

## 💡 实现提示

### 提示 1: 位操作技巧

```move
// 设置第 n 位为 1
value = value | (1 << n);

// 设置第 n 位为 0
value = value & !(1 << n);

// 获取第 n 位
bit = (value >> n) & 1;

// 提取多位（例如 bits 3-10）
extracted = (value >> 3) & 0xFF;

// 设置多位
value = (value & !0xFF00) | ((new_value & 0xFF) << 8);
```

### 提示 2: 性能测量

```typescript
// TypeScript 脚本测量 Gas
async function measureGas(
  client: AptosClient,
  account: AptosAccount,
  payload: any
): Promise<number> {
  const rawTxn = await client.generateTransaction(
    account.address(),
    payload
  );
  
  const simulation = await client.simulateTransaction(
    account,
    rawTxn
  );
  
  return parseInt(simulation[0].gas_used);
}
```

### 提示 3: 批量操作模式

```move
public entry fun batch_operation(
    signer: &signer,
    items: vector<Item>
) {
    // 1. 验证批次大小
    let len = vector::length(&items);
    assert!(len <= MAX_BATCH_SIZE, E_BATCH_TOO_LARGE);
    assert!(len > 0, E_EMPTY_BATCH);
    
    // 2. 预检查（如权限）
    verify_permissions(signer);
    
    // 3. 批量处理
    let i = 0;
    while (i < len) {
        process_item(vector::borrow(&items, i));
        i = i + 1;
    };
    
    // 4. 发出批量事件
    emit_batch_event(len);
}
```

---

## 📚 参考资源

### 官方文档
- [Aptos Gas Schedule](https://aptos.dev/concepts/gas-txn-fee/)
- [Move Gas Profiling](https://aptos.dev/guides/move-guides/gas-profiling/)

### 代码示例
- 查看 `01_理论学习/代码示例.move`
- 参考 `sources/` 下的实现

### 工具
```bash
# Gas Profiler
aptos move test --gas-profiler

# 交易模拟
aptos move run --function-id 0x1::module::function \
  --args ... \
  --gas-unit-price 100 \
  --max-gas 100000 \
  --profile default \
  --simulate
```

---

## ✅ 提交清单

在提交之前，确保：

- [ ] 所有代码可以编译通过
- [ ] 所有测试都通过
- [ ] Gas Profiler 运行成功
- [ ] 生成了优化报告
- [ ] 代码有充分注释
- [ ] 文档完整清晰

### 提交文件结构

```
02_实践任务/
├── sources/
│   ├── packed_storage.move
│   ├── compute_optimization.move
│   ├── batch_operations.move
│   └── gas_profiler.move
├── tests/
│   ├── storage_benchmark.move
│   ├── compute_benchmark.move
│   └── benchmark_suite.move
├── scripts/
│   └── analyze_gas.ts
└── docs/
    ├── storage_optimization.md
    ├── compute_optimization.md
    └── gas_optimization_report.md
```

---

## 🎯 挑战任务（可选）

如果你完成了基础任务，可以尝试这些挑战：

### 挑战 1: 自动优化建议工具

实现一个静态分析工具，扫描 Move 代码并给出 Gas 优化建议。

### 挑战 2: Gas 优化竞赛

选择一个现有的开源 Aptos 项目，找出 Gas 优化机会并提交 PR。

### 挑战 3: 优化方案库

创建一个 Gas 优化模式库，收集常见场景的最佳实践。

---

## ❓ 常见问题

**Q: 如何确定批次大小上限？**

A: 考虑：
- 单个操作的 Gas 成本
- 区块 Gas 限制
- 交易大小限制
- 用户体验（等待时间）

推荐：保守起见设置为能确保成功的值的 70%。

**Q: 何时应该使用数据打包？**

A: 当满足以下条件时：
- 多个小字段（bool, u8）
- 频繁一起访问
- 存储成本敏感
- 愿意接受额外的复杂度

**Q: 优化后测试失败怎么办？**

A: 检查：
1. 位操作逻辑是否正确
2. 边界情况是否处理
3. 是否有溢出风险
4. 对比未优化版本的行为

---

**准备好了吗？开始你的 Gas 优化之旅吧！🚀**

记住：优化是一个迭代过程，先让代码正确，再让代码快速！
