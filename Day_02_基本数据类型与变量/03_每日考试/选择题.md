````markdown
# Day 02 每日考试 - 选择题

**总分：75 分（每题 5 分）**  
**时间：30 分钟**

---

## 第 1 题

Move语言中，以下哪个**不是**基本整数类型？

A. `u8`  
B. `u32`  
C. `u64`  
D. `u128`

**答案：** 见答案解析

---

## 第 2 题

以下关于Move整数类型的说法，哪个是**正确**的？

A. Move支持有符号整数和无符号整数  
B. Move只支持无符号整数  
C. Move的整数可以是负数  
D. Move支持浮点数类型

**答案：** 见答案解析

---

## 第 3 题

`u8`类型的取值范围是多少？

A. -128 到 127  
B. 0 到 255  
C. 0 到 127  
D. -255 到 255

**答案：** 见答案解析

---

## 第 4 题

以下代码的输出是什么？

```move
let x: u64 = 1000;
let y: u8 = (x as u8);
```

A. y = 1000  
B. y = 255  
C. y = 232  
D. 编译错误

**答案：** 见答案解析

---

## 第 5 题

Move中变量默认的特性是什么？

A. 可变（mutable）  
B. 不可变（immutable）  
C. 取决于类型  
D. 必须显式声明

**答案：** 见答案解析

---

## 第 6 题

以下哪段代码会编译错误？

A. `let x = 10; let y = x;`  
B. `let mut x = 10; x = 20;`  
C. `let x = 10; x = 20;`  
D. `let x = 10; let x = 20;`

**答案：** 见答案解析

---

## 第 7 题

关于变量遮蔽（shadowing），以下说法哪个是**错误**的？

A. 可以在同一作用域重新声明同名变量  
B. 新变量会遮蔽旧变量  
C. 可以改变变量的类型  
D. 遮蔽会修改原来的变量

**答案：** 见答案解析

---

## 第 8 题

以下代码会发生什么？

```move
let x: u8 = 255;
let y = x + 1;
```

A. y = 256  
B. y = 0（溢出回绕）  
C. 运行时abort  
D. 编译错误

**答案：** 见答案解析

---

## 第 9 题

Move中的常量命名规范是什么？

A. camelCase  
B. snake_case  
C. UPPER_CASE  
D. PascalCase

**答案：** 见答案解析

---

## 第 10 题

以下哪个是正确的常量定义？

A. `let MAX_VALUE: u64 = 1000;`  
B. `const MAX_VALUE = 1000;`  
C. `const MAX_VALUE: u64 = 1000;`  
D. `constant MAX_VALUE: u64 = 1000;`

**答案：** 见答案解析

---

## 第 11 题

关于Move的复制（Copy）和移动（Move）语义，以下说法哪个是**正确**的？

A. 所有类型都会被复制  
B. 所有类型都会被移动  
C. 基本类型会复制，结构体会移动（除非有copy能力）  
D. 取决于编译器优化

**答案：** 见答案解析

---

## 第 12 题

以下代码会发生什么？

```move
struct Box { value: u64 }
let box1 = Box { value: 100 };
let box2 = box1;
let box3 = box1;
```

A. 编译通过，box1被复制两次  
B. 编译错误：box1已被移动  
C. 运行时错误  
D. box1、box2、box3都指向同一个对象

**答案：** 见答案解析

---

## 第 13 题

要让结构体可以被复制，需要什么能力？

A. `key`  
B. `copy`  
C. `drop`  
D. `store`

**答案：** 见答案解析

---

## 第 14 题

关于引用的借用规则，以下说法哪个是**错误**的？

A. 可以有多个不可变引用  
B. 可变引用同时只能有一个  
C. 可变引用和不可变引用可以同时存在  
D. 引用不能转移所有权

**答案：** 见答案解析

---

## 第 15 题

以下代码的输出是什么？

```move
let mut x = 10;
let ref_x = &mut x;
*ref_x = 20;
// x的值是多少？
```

A. 10  
B. 20  
C. 编译错误  
D. 未定义

**答案：** 见答案解析

---

## 第 16 题

元组`(u64, bool, address)`有几个元素？

A. 1  
B. 2  
C. 3  
D. 不确定

**答案：** 见答案解析

---

## 第 17 题

以下哪个是正确的元组解构？

```move
let triple = (1, true, @0x1);
```

A. `let (a, b, c) = triple;`  
B. `let [a, b, c] = triple;`  
C. `let {a, b, c} = triple;`  
D. `let a, b, c = triple;`

**答案：** 见答案解析

---

## 第 18 题

以下代码中，哪个操作是**合法**的？

```move
let addr: address = @0x1;
```

A. `let x = addr + 1;`  
B. `let x = addr * 2;`  
C. `let is_equal = (addr == @0x2);`  
D. `let x = addr / 2;`

**答案：** 见答案解析

---

## 第 19 题

整数除法 `10 / 3` 的结果是什么？

A. 3.33  
B. 3  
C. 4  
D. 3.0

**答案：** 见答案解析

---

## 第 20 题

以下哪个位运算是**错误**的？

A. `let x = 0b1010 & 0b1100;`  
B. `let x = 0b1010 | 0b1100;`  
C. `let x = 0b1010 << 2;`  
D. `let x = 0b1010 ** 2;`

**答案：** 见答案解析

---

## 第 21 题

关于`assert!`宏，以下说法哪个是**正确**的？

A. 第一个参数是错误码  
B. 第二个参数是条件  
C. 条件为true时程序中止  
D. 条件为false时程序中止

**答案：** 见答案解析

---

## 第 22 题

以下代码的问题是什么？

```move
public fun withdraw(balance: &mut u64, amount: u64) {
    *balance = *balance - amount;
}
```

A. 没有问题  
B. 缺少下溢检查  
C. 缺少类型注解  
D. 引用使用错误

**答案：** 见答案解析

---

## 第 23 题

`let mut x = 10;` 和 `let x = 10; let x = 20;` 的区别是什么？

A. 完全相同  
B. 前者修改变量，后者创建新变量  
C. 前者可以改变类型，后者不行  
D. 没有区别

**答案：** 见答案解析

---

## 第 24 题

以下哪个能力（ability）允许值被存储在全局存储中？

A. `copy`  
B. `drop`  
C. `key`  
D. `store`

**答案：** 见答案解析

---

## 第 25 题

短路求值中，以下哪个表达式不会执行`expensive_function()`？

A. `true && expensive_function()`  
B. `false && expensive_function()`  
C. `true || expensive_function()`  
D. B和C

**答案：** 见答案解析

---

## 第 26 题（附加题，10分）

以下代码的输出是什么？

```move
let mut x: u8 = 200;
x = x + 100;
let y: u64 = (x as u64);
```

A. y = 300  
B. y = 44  
C. 运行时abort  
D. 编译错误

**答案：** 见答案解析

---

## 第 27 题（附加题，10分）

以下哪种情况**不会**导致编译错误？

```move
struct Resource has key {
    value: u64
}
```

A. 将Resource赋值给另一个变量  
B. 将Resource存储到全局存储  
C. 复制Resource  
D. 在函数结束时丢弃Resource

**答案：** 见答案解析

---

## 计分说明

- 前 25 题每题 3 分，共 75 分
- 第 26-27 题附加题各 10 分
- **满分**：75 分（不含附加题）
- **及格分数**：53 分（70%）
- **优秀分数**：68 分（90%）

---

## 答题说明

1. 请在独立的纸上或文档中记录你的答案
2. 完成后对照 `答案解析.md` 检查
3. 仔细阅读解析，理解每道题的考点
4. 错题要重点复习相关知识点

**建议完成时间**：30 分钟  
**实际用时**：________

---

## 知识点分布

- **整数类型**：第 1-4, 8, 19-20 题
- **变量与可变性**：第 5-7, 15, 23 题
- **常量**：第 9-10 题
- **Move语义**：第 11-13 题
- **引用与借用**：第 14-15, 22 题
- **元组**：第 16-17 题
- **地址类型**：第 18 题
- **能力系统**：第 13, 24, 27 题
- **类型转换**：第 4, 26 题
- **逻辑运算**：第 21, 25 题

祝你考试顺利！🎓

````
