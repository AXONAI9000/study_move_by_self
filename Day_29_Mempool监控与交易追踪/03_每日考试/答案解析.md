# Day 29 每日考试 - 答案解析

## 选择题答案（40分）

### 第1题：B - 临时存储待确认交易
**解析**: Mempool 是内存池，用于临时存储已提交但尚未被打包进区块的交易。它不是永久存储，也不执行合约或验证区块。

---

### 第2题：C - 部分私有且快速确认
**解析**: 
- Aptos Mempool 默认部分私有（不公开所有交易）
- 平均出块时间约400毫秒，远快于以太坊的12秒
- 支持 Gas Price 竞价和 Block-STM 并行执行

---

### 第3题：C - 400毫秒
**解析**: Aptos 采用 AptosBFT 共识，平均出块时间约为400毫秒（亚秒级），这是其高性能的关键特性之一。

---

### 第4题：B - 签名、序列号、余额、过期时间等多项验证
**解析**: 交易进入 Mempool 前需要通过：
1. 签名验证
2. 序列号检查
3. 余额验证（Gas费用）
4. 过期时间检查
5. Gas 限制检查
6. Payload 验证

---

### 第5题：C - Gas Unit Price
**解析**: Mempool 中交易主要按 Gas Unit Price 排序，价格越高优先级越高。同一发送者的交易还需按序列号顺序执行。

---

### 第6题：B - 交易还在 Mempool 或不存在
**解析**: 查询交易返回404表示：
- 交易还在 Mempool 中等待打包
- 交易不存在或哈希错误
- 需要继续等待或检查交易是否已被丢弃

---

### 第7题：B - POST /v1/transactions
**解析**: 
- POST /v1/transactions 用于提交交易
- GET /v1/transactions 用于查询交易列表
- 其他方法不用于交易提交

---

### 第8题：C - 必须大于等于当前序列号且不能过大
**解析**: 序列号规则：
- 必须 >= 当前账户序列号
- 不能超过当前序列号 + MAX_SEQUENCE_GAP（通常100）
- 必须按顺序执行

---

### 第9题：B - 使用合理间隔的轮询或订阅
**解析**: 
- 过于频繁的查询会造成API压力
- 合理间隔（如2-5秒）的轮询
- 或使用 WebSocket 订阅更好

---

### 第10题：D - CACHED（已缓存）
**解析**: 交易的最终状态包括：
- SUCCESS（成功执行并上链）
- FAILED（执行失败但已上链）
- DROPPED（被丢弃，未上链）
- CACHED 不是交易状态

---

### 第11题：B - Maximal Extractable Value
**解析**: MEV = Maximal Extractable Value（最大可提取价值），指通过重新排序、插入或审查交易来提取的价值。

---

### 第12题：B - DEX 套利
**解析**: 
- ✅ DEX 套利：利用公开市场价格差异，合规
- ❌ Front-running：抢跑用户交易，不合规
- ❌ 三明治攻击：损害用户利益，不合规
- ❌ 操纵价格：市场操纵，违法

---

### 第13题：B - 比较不同 DEX 的价格差异
**解析**: 套利的核心是发现价格差异：
1. 获取多个 DEX 的价格
2. 比较同一交易对的价格
3. 计算价差是否超过成本（Gas + 手续费）
4. 执行套利交易

---

### 第14题：B - 交易提交到上链的时间
**解析**: 确认时间 = 交易上链时间 - 交易提交时间，通常用于衡量网络性能和交易优先级。

---

### 第15题：C - 避免无限等待未确认的交易
**解析**: 设置超时的原因：
- 交易可能因 Gas 不足被丢弃
- 交易可能过期
- 网络可能拥堵
- 避免程序阻塞

---

### 第16题：B - 减少API调用次数和提高吞吐量
**解析**: 批量处理的好处：
- 减少网络请求次数
- 提高整体吞吐量
- 降低API服务器负载
- 更高效的资源使用

---

### 第17题：B - 减少重复查询提高性能
**解析**: 缓存机制：
- 避免重复查询相同数据
- 减少网络延迟
- 降低服务器负载
- 提高响应速度

---

### 第18题：B - 复用连接减少开销
**解析**: 连接池：
- 预先创建多个连接
- 复用连接避免频繁建立/销毁
- 提高并发处理能力
- 减少连接开销

---

### 第19题：B - 实现重试机制和错误恢复
**解析**: 网络错误处理：
- 实现指数退避重试
- 记录错误日志
- 切换备用节点
- 保持系统稳定运行

---

### 第20题：D - 代码行数
**解析**: 性能监控关键指标：
- 处理延迟（响应时间）
- 吞吐量（TPS）
- 错误率
- CPU/内存使用率
- 代码行数与性能无关

---

## 编程题答案（60分）

### 第1题：实现简单的交易监控器（20分）

```typescript
import { AptosClient } from 'aptos';

class SimpleMonitor {
  private client: AptosClient;
  private intervalId: NodeJS.Timer | null = null;
  private lastVersion: number = 0;
  private transactionCount: number = 0;
  private targetAddress: string = '';

  constructor(nodeUrl: string) {
    this.client = new AptosClient(nodeUrl);
  }

  async start(address: string, interval: number): Promise<void> {
    this.targetAddress = address;
    
    // 获取当前 ledger version
    try {
      const ledger = await this.client.getLedgerInfo();
      this.lastVersion = Number(ledger.ledger_version);
    } catch (error) {
      console.error('Failed to get ledger info:', error);
      throw error;
    }

    // 开始轮询
    this.intervalId = setInterval(() => this.poll(), interval);
    console.log(`Monitoring started for address: ${address}`);
  }

  stop(): void {
    if (this.intervalId) {
      clearInterval(this.intervalId);
      this.intervalId = null;
      console.log('Monitoring stopped');
    }
  }

  getTransactionCount(): number {
    return this.transactionCount;
  }

  private async poll(): Promise<void> {
    try {
      const ledger = await this.client.getLedgerInfo();
      const currentVersion = Number(ledger.ledger_version);

      if (currentVersion > this.lastVersion) {
        // 获取新交易
        const limit = Math.min(currentVersion - this.lastVersion, 100);
        const transactions = await this.client.getTransactions({
          start: this.lastVersion + 1,
          limit
        });

        // 过滤目标地址的交易
        for (const tx of transactions) {
          if ((tx as any).sender === this.targetAddress) {
            this.transactionCount++;
            console.log(`Found transaction: ${(tx as any).hash}`);
          }
        }

        this.lastVersion = currentVersion;
      }
    } catch (error) {
      console.error('Poll error:', error);
    }
  }
}

// 测试
async function test() {
  const monitor = new SimpleMonitor('https://fullnode.testnet.aptoslabs.com/v1');
  await monitor.start('0x1', 2000);
  
  // 运行10秒后停止
  setTimeout(() => {
    monitor.stop();
    console.log(`Total transactions found: ${monitor.getTransactionCount()}`);
  }, 10000);
}

// test();
```

**评分要点**:
- ✅ 正确使用 AptosClient (2分)
- ✅ 实现轮询逻辑 (6分)
- ✅ 正确过滤交易 (6分)
- ✅ 正确启动和停止 (4分)
- ✅ 错误处理 (2分)

---

### 第2题：实现交易状态追踪器（20分）

```typescript
import { AptosClient } from 'aptos';

enum TxStatus {
  PENDING = 'pending',
  CONFIRMED = 'confirmed',
  TIMEOUT = 'timeout'
}

interface TrackResult {
  hash: string;
  status: TxStatus;
  submitTime: number;
  confirmTime?: number;
  confirmationDuration?: number;
}

class StatusTracker {
  private client: AptosClient;

  constructor(nodeUrl: string) {
    this.client = new AptosClient(nodeUrl);
  }

  async track(txHash: string, timeout: number): Promise<TrackResult> {
    const submitTime = Date.now();
    const deadline = submitTime + timeout;

    const result: TrackResult = {
      hash: txHash,
      status: TxStatus.PENDING,
      submitTime
    };

    // 轮询检查交易状态
    while (Date.now() < deadline) {
      try {
        const tx = await this.client.getTransactionByHash(txHash);
        
        // 交易已确认
        const confirmTime = Date.now();
        result.status = TxStatus.CONFIRMED;
        result.confirmTime = confirmTime;
        result.confirmationDuration = confirmTime - submitTime;
        
        return result;
      } catch (error: any) {
        // 404 表示交易还在 Mempool
        if (error.status === 404) {
          // 等待1秒后重试
          await this.sleep(1000);
          continue;
        }
        
        // 其他错误抛出
        throw error;
      }
    }

    // 超时
    result.status = TxStatus.TIMEOUT;
    return result;
  }

  private sleep(ms: number): Promise<void> {
    return new Promise(resolve => setTimeout(resolve, ms));
  }
}

// 测试
async function test() {
  const tracker = new StatusTracker('https://fullnode.testnet.aptoslabs.com/v1');
  
  // 需要一个真实的交易哈希进行测试
  const txHash = '0x...'; // 替换为真实交易哈希
  
  try {
    const result = await tracker.track(txHash, 30000);
    console.log('Track result:', result);
  } catch (error) {
    console.error('Tracking failed:', error);
  }
}

// test();
```

**评分要点**:
- ✅ 正确查询交易状态 (8分)
- ✅ 正确处理超时 (6分)
- ✅ 正确计算确认时间 (4分)
- ✅ 错误处理和代码质量 (2分)

---

### 第3题：实现套利机会扫描器（20分）

```typescript
interface Price {
  dex: string;
  tokenPair: string;
  price: number;
}

interface Opportunity {
  dexA: string;
  dexB: string;
  tokenPair: string;
  priceA: number;
  priceB: number;
  profitPercent: number;
  estimatedProfit: number;
}

class ArbitrageScanner {
  scan(prices: Price[], minProfitPercent: number): Opportunity[] {
    const opportunities: Opportunity[] = [];
    
    // 按交易对分组
    const pairGroups = new Map<string, Price[]>();
    for (const price of prices) {
      if (!pairGroups.has(price.tokenPair)) {
        pairGroups.set(price.tokenPair, []);
      }
      pairGroups.get(price.tokenPair)!.push(price);
    }

    // 对每个交易对，比较所有 DEX 的价格
    for (const [pair, priceList] of pairGroups) {
      for (let i = 0; i < priceList.length; i++) {
        for (let j = i + 1; j < priceList.length; j++) {
          const priceA = priceList[i];
          const priceB = priceList[j];

          // 计算价格差异百分比
          const priceDiff = Math.abs(priceA.price - priceB.price);
          const avgPrice = (priceA.price + priceB.price) / 2;
          const profitPercent = (priceDiff / avgPrice) * 100;

          // 检查是否满足最小利润要求
          if (profitPercent >= minProfitPercent) {
            // 计算实际收益（假设交易10000 USDC）
            const tradeAmount = 10000;
            const feePercent = 0.3; // 0.3% 手续费
            const estimatedProfit = this.calculateProfit(
              priceA.price,
              priceB.price,
              tradeAmount,
              feePercent
            );

            opportunities.push({
              dexA: priceA.dex,
              dexB: priceB.dex,
              tokenPair: pair,
              priceA: priceA.price,
              priceB: priceB.price,
              profitPercent,
              estimatedProfit
            });
          }
        }
      }
    }

    // 按预期收益排序
    opportunities.sort((a, b) => b.estimatedProfit - a.estimatedProfit);

    return opportunities;
  }

  calculateProfit(
    priceA: number,
    priceB: number,
    tradeAmount: number,
    feePercent: number
  ): number {
    // 确定买入和卖出价格
    const buyPrice = Math.min(priceA, priceB);
    const sellPrice = Math.max(priceA, priceB);

    // 计算购买数量
    const buyAmount = tradeAmount / buyPrice;
    
    // 计算手续费（两次交易）
    const buyFee = tradeAmount * (feePercent / 100);
    const sellFee = (buyAmount * sellPrice) * (feePercent / 100);
    const totalFee = buyFee + sellFee;

    // 计算毛利润
    const revenue = buyAmount * sellPrice;
    const cost = tradeAmount;
    const grossProfit = revenue - cost;

    // 计算净利润（扣除手续费，这里简化了 Gas 费用）
    const netProfit = grossProfit - totalFee;

    return Number(netProfit.toFixed(2));
  }
}

// 测试
function test() {
  const scanner = new ArbitrageScanner();

  const prices: Price[] = [
    { dex: 'Liquidswap', tokenPair: 'APT/USDC', price: 10.0 },
    { dex: 'PancakeSwap', tokenPair: 'APT/USDC', price: 10.5 },
    { dex: 'SushiSwap', tokenPair: 'APT/USDC', price: 9.8 },
    { dex: 'Liquidswap', tokenPair: 'ETH/USDC', price: 2000 },
    { dex: 'PancakeSwap', tokenPair: 'ETH/USDC', price: 2005 }
  ];

  const opportunities = scanner.scan(prices, 1.0);
  
  console.log('Found opportunities:', opportunities.length);
  for (const opp of opportunities) {
    console.log(`\n${opp.dexA} vs ${opp.dexB} (${opp.tokenPair})`);
    console.log(`Price A: ${opp.priceA}, Price B: ${opp.priceB}`);
    console.log(`Profit: ${opp.profitPercent.toFixed(2)}% (~$${opp.estimatedProfit})`);
  }
}

// test();
```

**评分要点**:
- ✅ 正确比较价格 (6分)
- ✅ 正确计算利润百分比 (6分)
- ✅ 正确计算实际收益（考虑费用）(6分)
- ✅ 代码质量和逻辑清晰 (2分)

**输出示例**:
```
Found opportunities: 2

PancakeSwap vs SushiSwap (APT/USDC)
Price A: 10.5, Price B: 9.8
Profit: 6.93% (~$631.51)

Liquidswap vs PancakeSwap (ETH/USDC)
Price A: 2000, Price B: 2005
Profit: 0.25% (~$38.94)
```

---

## 成绩评定标准

### 选择题（40分）
- 36-40分：优秀 ⭐⭐⭐⭐⭐
- 32-35分：良好 ⭐⭐⭐⭐
- 28-31分：及格 ⭐⭐⭐
- <28分：需要重学

### 编程题（60分）
- 54-60分：优秀 ⭐⭐⭐⭐⭐
- 48-53分：良好 ⭐⭐⭐⭐
- 42-47分：及格 ⭐⭐⭐
- <42分：需要重学

### 总分（100分）
- 90-100分：优秀 ⭐⭐⭐⭐⭐ - 完全掌握
- 80-89分：良好 ⭐⭐⭐⭐ - 掌握较好
- 70-79分：及格 ⭐⭐⭐ - 基本掌握
- <70分：需要重学 - 回顾理论和实践

---

## 学习建议

### 如果得分 < 70分
1. 重新阅读理论学习材料
2. 仔细研究代码示例
3. 重做实践任务
4. 重新参加考试

### 如果得分 70-85分
1. 复习错题相关知识点
2. 优化编程题代码
3. 阅读扩展资料
4. 准备学习 Day 30

### 如果得分 > 85分
1. 恭喜！可以继续学习 Day 30
2. 可以尝试加分项任务
3. 分享你的学习心得
4. 帮助其他同学

---

**继续加油！🚀**
