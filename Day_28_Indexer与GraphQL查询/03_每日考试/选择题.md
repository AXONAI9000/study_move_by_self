# Day 28 每日考试 - 选择题

**考试时间**: 30 分钟  
**总分**: 40 分（每题 2 分）  
**及格分数**: 28 分

---

## 📝 答题说明

- 每题只有一个正确答案
- 请在答题纸上写下题号和你选择的选项（A/B/C/D）
- 完成后对照答案解析文件检查

---

## 第一部分：Indexer 架构（8题，16分）

### 1. Aptos Indexer 的主要作用是什么？
A. 执行智能合约  
B. 将区块链数据转换为易查询的结构化格式  
C. 验证交易签名  
D. 管理账户余额  

### 2. Indexer 数据处理的正确流程是？
A. Database → Processor → Fullnode → GraphQL  
B. Fullnode → GraphQL → Processor → Database  
C. Fullnode → Transaction Stream → Processor → Database → GraphQL  
D. GraphQL → Fullnode → Database → Processor  

### 3. 哪个组件负责监听 Fullnode 的新交易？
A. Processor  
B. Transaction Stream Service (TSS)  
C. GraphQL Engine  
D. PostgreSQL  

### 4. Coin Processor 主要处理什么数据？
A. NFT 所有权  
B. 代币余额和转账  
C. 智能合约代码  
D. 用户账户信息  

### 5. Indexer 的数据延迟通常是多少？
A. 小于 10ms  
B. 10-50ms  
C. 400-500ms  
D. 5-10秒  

### 6. Indexer 提供什么类型的一致性保证？
A. 强一致性  
B. 最终一致性  
C. 因果一致性  
D. 不保证一致性  

### 7. Hasura GraphQL Engine 的作用是？
A. 存储区块链数据  
B. 处理交易  
C. 基于数据库 Schema 自动生成 GraphQL API  
D. 验证签名  

### 8. 多个 Processor 可以并行工作吗？
A. 可以，它们独立处理不同类型的数据  
B. 不可以，必须串行执行  
C. 只有在测试网可以  
D. 取决于网络状况  

---

## 第二部分：GraphQL 语法（8题，16分）

### 9. GraphQL 的三种主要操作类型是？
A. Get, Post, Delete  
B. Query, Mutation, Subscription  
C. Select, Insert, Update  
D. Read, Write, Listen  

### 10. Aptos Indexer 支持哪些 GraphQL 操作？
A. 只支持 Query  
B. 只支持 Query 和 Mutation  
C. Query 和 Subscription  
D. 全部支持  

### 11. 以下哪个是正确的 GraphQL 变量定义？
A. `query GetTransactions($limit: Int) { ... }`  
B. `query GetTransactions(limit: Int!) { ... }`  
C. `query GetTransactions($limit: Int!) { ... }`  
D. `query GetTransactions{$limit: Int!} { ... }`  

### 12. GraphQL 片段（Fragment）的作用是？
A. 分割查询提高性能  
B. 复用查询字段结构  
C. 加密查询数据  
D. 缓存查询结果  

### 13. 在 GraphQL 中，如何表示"大于"条件？
A. `{field: {>: value}}`  
B. `{field: {_gt: value}}`  
C. `{field: {greater: value}}`  
D. `{field: {gt: value}}`  

### 14. 以下哪个查询会返回最新的交易（降序）？
A. `order_by: {version: asc}`  
B. `order_by: {version: desc}`  
C. `sort: {version: -1}`  
D. `order: "version DESC"`  

### 15. GraphQL 别名（Alias）的语法是？
A. `recent = transactions { ... }`  
B. `recent: transactions { ... }`  
C. `transactions as recent { ... }`  
D. `transactions(alias: "recent") { ... }`  

### 16. 如何在 GraphQL 中实现模糊匹配？
A. `{field: {_eq: "%pattern%"}}`  
B. `{field: {_like: "%pattern%"}}`  
C. `{field: {_match: "pattern"}}`  
D. `{field: {_regex: ".*pattern.*"}}`  

---

## 第三部分：数据模型（4题，8分）

### 17. transactions 表的主键是？
A. hash  
B. sender  
C. version  
D. timestamp  

### 18. events 表的主键是？
A. 只有 transaction_version  
B. 只有 event_index  
C. transaction_version 和 event_index 的组合  
D. type 和 sequence_number 的组合  

### 19. current_coin_balances 表存储的是？
A. 历史所有余额记录  
B. 当前最新的余额  
C. 待处理的余额变化  
D. 代币的元数据  

### 20. 哪个表用于存储 NFT 的当前所有权？
A. tokens  
B. token_ownerships  
C. current_token_ownerships_v2  
D. nft_balances  

---

**做完后请前往答案解析文件核对答案！**
